# 更新日志

本文件记录对公共技能套件的可见改动。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

规则分级变化按其对创作者的约束力记录：`structural_invariant` 与 `reviewed_invariant`
的新增或收紧记为 `Changed`（可能阻断既有产物），`craft_default` 与 `taste_option` 记为
`Added`（可被创作者覆盖）。

自 `v0.6.2` 起，小节名采用 Keep a Changelog 的六个英文类别（`Added` / `Changed` /
`Deprecated` / `Removed` / `Fixed` / `Security`），正文仍为中文；`v0.6.1` 及更早的小节
保持原样。

## [Unreleased]

### Fixed

**参考图的 `控制` 是肯定句在起作用。** 实测把「正文怎么描述这张参考」当唯一变量跑了 20 次：
只写 `fully_preserved`、或写「不得控制构图」，构图都不会被带过来；写「保留起始构图、站位、机位高度」
才会，而且很准。所以需要保住构图的槽位（典型是 `用途：起始帧`）必须在 `控制` 里点名构图，
漏写就丢掉这个锚点，别处的 `不得控制` 补不回来；`不得控制` 仍写，但它约束的是 owner 与 reviewer
的责任边界，不是模型。

**起始帧走 full-reference 不会丢锚点。** 方言要求「起始帧 + 角色板」整组以 `reference_image` 送入，
此前没验证过这条会不会静默丢掉已接受的开场状态。用一张极难碰巧复现的构图测三条路径各 3 次，
9 次全部忠实还原开场，指示成立。

**连续段的尾帧那一半已验证。** 绑上一段的真实尾帧并在正文要求保留开场状态，下一段开场与尾帧高度一致；
纯文字续写则完全换了场景。绑真实视频那一半在本次执行端未开通，仍未验证。

## [0.6.6] - 2026-09-08

本版第一次用真实生成去核对套件里「模型会怎样」这一类说法：四条没有复现，几条被确认并给出了数字。
最直接可用的一条是对白容量——H3 中文约 4.1 字/秒，5 秒镜约 20 字，超出太多会静默截断。

### Added

**对白先估时再定镜长。** 分镜在「声音」中简写录音实测或文本估计的依据，区分对白、停顿、并行动作与
收尾反应；视频提示词按实际可用发声窗口复核。文本估计采用区间，按较慢一端选容得下的合法镜长，
放不下回分镜延长或拆镜。这是可覆盖的 `craft_default`，不新增固定每秒字数的机械门槛（#106）。

### Changed

**全量复审现在会对无对白镜的声音段提出修订。** 审查表新增了画音同轨的核对项（`VID-22`、`VID-23`）：
用 `No dialogue`、`无对白` 这类排除句代替正面声景、完整性声明枚举本镜并没有的层、以及非画内配乐层
整个缺席，现在都会被报成 finding。

*升级*：**既有《视频提示词.md》里用这类写法的镜头，本版之后复审会给 `REVISE`**，按下面 Fixed 里的
正面写法改写即可——写出声轨的构成与覆盖区间、把「不说话」写成在场人物的可见状态、完整性声明只列
本镜实际有的层、非画内配乐层显式写空（H3 为 `non_diegetic_music: N/A`）。本条只改变审查结论，
不改目录、字段名或文档结构，也不要求重跑任何已生成的媒体。

### Fixed

**H3 对白回到主时间线。** 原方言把对白与环境声一起交给声音段，现在按官方 Base/Full-reference 指南，
逐字对白只放在 `integrated_multimodal_description` / `detailed_description` 的对应事件，
`overall_soundscape` 汇总环境与音效。补清 H3 的任意整数秒设置、语速提示的能力边界、音频参考与复用的
差别，以及 Context-IR 仍需预先指定时长；秒段加总通过不再被当作自然语速可行的证据（#106）。

**Seedance 时长按任务解释。** 2.0/2.5 同样复核对白预算，补清自动时长 `-1` 与 2.5 编辑保持源片时长的
区别。2.0 使用镜头序号，2.5 使用整数秒时间戳；不把时间码或响应里的取整秒数当作精确发声保证（#106）。

**无对白镜的声音段改为正面写法。** `overall_soundscape` 写出声轨的构成与覆盖区间，主时间线把
「不说话」写成在场人物的可见状态，完整性声明只列本镜实际有的层，不写 `No dialogue`、`无对白`。
理由是正文要如实、完整地交代本镜声音，不是因为排除句会招来台词——那条推测没有复现，见下（#108）。

**参考图、画内文字与时间戳也做了实测。** 画内可读中文可以逐字指定（要求牌子写「第三仓库」，
H3 三次、Seedance 2.5 一次，字形全对）；`用途：身份` 的角色板对身份与造型的控制很强；
Seedance 2.5 稳定响应整数秒时间戳而 2.0 不稳定（两次里一次提前三秒），与既有方言描述一致。
角色板「顺带决定构图」这种泄漏本轮**没能造出来**（12 次，含 30° 斜角超低机位的参考），
所以 `不得控制` 的写法既未被证实也未被证伪——本轮没有覆盖 `用途：起始帧` 这类本就该控制构图的槽位（#110）。

**几条「模型会怎样」的说法按实测改写。** 本版第一次用真实生成核对了套件里的模型行为断言，
逐条结论与样本量由维护者在本地实测记录中保留，未入库。据此修订：不写非画内配乐会被
模型补上配乐（**成立**，`non_diegetic_music` 一行不能省）；不写语音层会被补台词、否定句里的名词会被
画出来、**视频**不写禁字幕会带烧录字幕（**均未复现**，相关写法保留但不再作为已知缺陷断言）。
注意方向相反的一条：**图片**不写禁文字确实会长出可读文字（`gpt-image-2` 实测，加上约束即干净），
所以图片阶段的文字政策照旧有据可依，只有视频阶段那一份是预防性的。Seedance 的 `{角色用中文说："…"}` 与 H3 的
`<d>[Chinese] …</d>` 都能拿到逐字台词（**成立**）。

**对白容量有了实测数字，并修正上一条结论。** 上一版说「镜长装不下的台词会被赶语速而不是截断」，
把 5–13 秒、7–108 字铺开测完之后，这句话只在一段范围内成立：H3 中文自然语速约 **4.1 字/秒**
（容量约 `4.1 × 镜长`，5 秒镜约 20 字）；超出后先赶语速，最快到自然语速的 2.5 倍且仍然完整；
**再超过约 9 字/秒就静默截断**——送 78 字与送 108 字都只说了 46 字，接口返回成功、不报错。
「秒段加总正确」因此不能当作台词说得完的证据。另外确认「放不下就延长镜头」这个办法真的有效：
37 字从 5 秒改到 10 秒、50 字从 5 秒改到 13 秒，语速都回到自然且台词完整（#110）。

## [0.6.5] - 2026-09-04

本版把两件事落到文档上：这一镜要送哪几张图，以及这一镜从剧本的哪一段来。既有项目需要按下面的
《升级》给分镜补两类字段；补完之后不改目录，也不动已有正文。

### Added

**参考图可以由创作者自己挂载。** 新增 `PLAN-...` 槽位，语法与 `REF-...` 逐字相同，只把定位符从项目
相对路径换成本项目已有的条目 ID：`IMG-...` 指《图片提示词.md》的板子，`SHOT-...` 指那一镜的冻结关键帧。
它声明的是「这张图不在项目里，生成时由创作者按这个顺序挂载」，与「已有图片」和「待补」都不是同一句话。
绑了 `PLAN-...` 的镜头照常写成 `生成方式：图生视频` 并产出最终《视频提示词.md》，`顺序` 就是挂图次序，
也是可复制正文里 `<Picture N>` 的编号；`REF-...` 和 `PLAN-...` 可以在同一条声明里混用，`顺序` 统一连续编号。

此前「输入参考图」只有三种准备结果，其中两种不带任何图片引用：`待补` 会阻止最终视频提示词，
`无（创作者已明确选择文生视频）` 则让 H3 退回三段 Base 正文，既没有 `<Picture N>` 也没有
`subject_definitions`。于是在自己的界面里出图、图片从不进项目的创作者，无论选哪一种都拿不到
「本镜送哪一张起始帧、哪些人物/道具/场景图」这个答案——而这正是 #92 当初要的东西。
可复制正文本来就只依赖 `顺序` 和 `用途`，从不依赖文件路径，所以这条正文一直写得出来，
挡住它的是就绪规则，不是提示词语法（#92、#101）。

`PLAN-...` 不能作为视频生产的输入：本套件没有文件可送，`$short-drama-produce` 的 `prepare` 直接失败
并说明先把真实文件放进项目、改写成 `REF-...`，不拿计划当输入，也不静默丢掉参考。渲染某一镜起始画面的
那个图片 job 不受影响——它从冻结关键帧正文出图，本来就不发送这些参考，和「待补参考图」时的行为一致，
所以写了挂图计划之后仍然可以让套件把起始帧渲染出来。

### Changed

**分镜的每一镜要说清它来自剧本的哪一场，剧本的每一场也要有镜头认领。** `creator_markdown_check.py`
机械核对三件事：每镜「来源」解析到《剧本.md》真实存在的场景 ID；剧本里每个场景都被某一镜认领，
或写进《分镜.md》正文开头的 `- 未拍场次：<场景 ID>（理由：……）`；同一场景不能既被认领又记为未拍。
此前校验器只要求《剧本.md》存在，从不打开它——把每镜「来源」全指向不存在的场景、再整份替换剧本，
校验仍然报 OK。SHT-01 从 0.1 起就是 `structural_invariant`，也一直允许带理由的省略，但成稿文档里
省略和漏拍长得一模一样，理由只留在对话里，于是这条不变量没有任何产物能证明它执行过（#100）。

**可复制正文里的中文引文必须来自上游文档。** 被引号或 `<d>` 包起来的中文，四个汉字以上的连续片段
必须能在《剧本.md》《视觉设定.md》或《分镜.md》里逐字找到（比对时忽略标点与空白，正文换行不影响判定）。
台词的逐字义务本来就写在视频提示词技能里，但没有落点，提示词可以给角色补一句剧本没写过的话而不被发现。
三个字以内的引文是画内标牌和语气词远多于台词，机械核对不碰它们，这部分仍由审查承担；规则记为 `VID-25`。

*升级*：既有《分镜.md》每镜补一行 `- 来源：<场景 ID>`，ID 与《剧本.md》的 `## <场景 ID> …` 标题一致，
跨场次的镜头用 `、` 连写多个；确实决定不拍的场次在文档开头补
`- 未拍场次：<场景 ID>（理由：……）`，多项用 `；` 连接。《剧本.md》的场景标题同时收紧为
`## <场景 ID> 内|外|内外 · 地点 · 时间/天气`，场景 ID 形如 `EP001-SC001`；解析不到任何场景标题会被指名报错。
《视频提示词.md》里剧本没有写过的台词回到 `$short-drama-write` 改剧本，不在提示词里改。

**`身份` 到底带不带服装。** 《参考角色》的表把 `身份` 的默认不可控写成「临时服装」，MiniMax H3 方言
写成「只锚定脸型体态」，而《五份创作文档》的示例又把 `本集造型` 写进 `身份` 的 `控制` 里。实跑的
agent 在这里明确犹豫过。界线现在写清楚：分界是「本集之内会不会变」，不是「算不算衣服」——角色板同时
固定长相和本集不变的造型时写一个 `身份` 槽位并把 `本集造型` 列进 `控制`；本集之内会改变的换装、湿透、
破损才另开 `造型状态`。判断依据永远是槽位自己写下的 `控制`，不是用途名称的字面意思。

**`assets/video-prompts.md` 属于哪条管线。** 它是结构化管线（`motion-specs.jsonl` 一路）的输出模板，
字段与 creator-first 的《视频提示词.md》骨架并不相同，实跑的 agent 一度不确定该跟哪一份。文件开头
现在直接写明这一点，并列出 creator-first 的字段顺序。

**道具板该写哪个 `用途`。** 九个取值原先全部用人物的说法描述（`身份` 写成「只锚定脸型体态」），
道具板落在哪一个只能猜。《参考角色》补上判据：跨镜不变的形制、配色、裂纹与磕角写 `身份`，
会随剧情改变的亮屏/熄屏、开合、湿污、破损写 `造型状态`，两者都要固定时拆成两个槽位。

### Fixed

**「待补参考图」里用括号点名条目时，报错又报到了别处。** 缺口清单写成
「江晨人物图（IMG-JIANGCHEN-SHEET）、本镜起始帧」时，判断「无（…）」的正则在第一个右括号处停下，
整条注记不再被认成合法的「无」，于是诊断变成「输入参考图必须使用完整 REF 语法」。它最直接的修法
仍然是删掉缺口那一段——正好恢复 #92 报告的静默降级，与 0.6.4 修 `；` 分隔符时是同一类缺陷。
现在注记里可以出现成对括号。

**视频提示词阶段契约与它自己的工作流互相矛盾。** 契约写「不回写分镜或视觉设定」，工作流第 2、3 步
却要求在同一请求内刷新分镜的「输入参考图」，必要时一并回填「视觉依据」。契约现在写明这个例外：
那是分镜 owner 在本请求内的动作，只更新这两条依据字段，不动镜头边界、职责、起终状态或关键帧正文。

**没有参考图时给出的选项少了一条，而少掉的那条正是最该给的。** 项目里一张图都没有时，
路由和视频提示词技能只让创作者在「先生成参考图」和「明确改走文生视频」之间选。前者要走
`$short-drama-produce`，需要项目外的 adapter 配置与供应商凭据，而套件自带的唯一图片 adapter 是
GPT Image 2；只用网页界面出图的创作者两条路都走不通，于是停在视频提示词之前（#101）。
现在三条路一起给出：把已有图片放进项目绑成 `REF-...`、由创作者自己出图并写成 `PLAN-...` 挂图计划、
或者明确改走文生视频。用本套件生产只是第一条路的一种做法，不再被写成唯一入口——
分镜技能和 MiniMax H3 方言里「起始帧只能由 produce 渲染」的说法同步改掉。
缺口清单本身就是挂图计划的草稿，所以停下来的那一次报告现在直接给出逐镜要挂哪几张、每张管什么。

**分镜读的是对话里的剧本草稿还是文件里的剧本，从来没有写明。** 创作者说「剧本已确认采用」指的是
`剧集/<EP>/剧本.md` 这个文件，而分镜技能只写「当前剧本加必要视觉事实即可直接进入」，没有任何一句
要求开始前重读该文件。现在写明以文件当前内容为准，与对话中更早的草稿不一致时说出差异（#100）。

### 已知缺口

`PLAN-...` 的定位符只能是 `IMG-...` 或 `SHOT-...`。项目还没有《图片提示词.md》时，创作者自备的
人物图和场景图没有可写的定位符，只能先补图片提示词条目或改走 `REF-...`。让 `PLAN-...` 直接定位
《视觉设定.md》条目是自然的下一步，本版不做。

## [0.6.4] - 2026-09-02

本版收紧两条创作文档契约，既有项目需要按下面的《升级》补两类字段；补完之后不再改目录，也不动已有正文。
主线是让「画面里有谁」和「这一镜送哪几张图」都写在文档上、能被机械核对，并让内置视频 adapter
真的跑得完一次生成。

### Added

**MiniMax H3 多图参考的编号与模式选择。** 方言文件写明模式由 `用途` 组合决定（只有起始帧走首帧，
起始帧加结束帧走首尾帧，起始帧与人物/地点/道具图同时存在或只有这些图走 full-reference），并写明
首/尾帧输入与参考输入互斥时整组统一走 full-reference。正文素材标签 `<Picture N>` 按同类素材的
`顺序` 编号，与生产端绑定顺序一致；`subject_definitions` 把主体绑到标签，`retention_analysis` 用
`fully_preserved` / `partially_preserved` / `attribute_transfer` / `weak_reference`（音频用
`fully_copy` / `partially_copy` / `reference` / `weak_reference`）写保留强度，并给出一个完整例子。
同时补上官方素材上限（1 首帧、1 尾帧、9 参考图、3 参考视频、3 参考音频，视频与音频每段 2–15 秒）
和 `MiniMax-H3-Max` 的 5 秒时长下限。只给尾帧的 L2VA 模式本套件不使用，理由写在方言文件里。

**分镜的冻结关键帧可以直接投产成图片。** `分镜.md` 成为 image 生产源：job 的 `source` 指向它、
`source_entry` 写本镜 `SHOT-...`，正文取该镜的 `### 冻结关键帧提示词`，产出落在
`剧集/<EP>/制作成果/images/`。此前每一镜的起始画面只存在于分镜的提示词里，没有任何阶段能把它渲染成
文件，于是 `用途：起始帧` 指向的那张图在套件内无处可来。选择器与文档一一对应：`图片提示词.md` 只收
`IMG-*`，`视频提示词.md` 只收 `MOTION-*`，`分镜.md` 只收 `SHOT-*`。

**会话里点名的目标模型要落进项目档案。** 创作者说“按 MiniMax H3 写”而 `production_profile` 仍是
`unset` 时，先把这个选择连同原生时长、参考方式和正文语言写进 `short-drama.json`，再进入下游阶段；
`$short-drama` 补上了可以照着跑的 `publish` → `accept` → `set-authority` 三步和决策记录的样子。

**一句话把范围说清楚。** README 与 creator-first 工作流给出示例请求，说明一句话值得交代的四件事：
做哪一集的哪一阶段、目标视频模型、参考图在哪里或还没有、以及没有图时是等图还是明确走文生视频。

### Changed

**冻结关键帧里的人和物必须在「视觉依据」里对得上。** 关键帧写完之后，本镜可见且需要保持身份、
造型或地理的人物、地点和道具逐项写进每镜必写的「视觉依据」，条目名称解析到《视觉设定.md》真实标题。
`creator_markdown_check.py` 机械核对三件事：字段在、条目解析得到、以及**关键帧正文点名的条目必须出现在
本镜「视觉依据」里**。点名靠《视觉设定.md》条目自身的名称，或该条目声明的 `画面代称`——提示词正文用哪个
拼写叫它。此前这条只是 `SHT-22` 一行 `reviewed_invariant` 加一句 `应`，与「不生成覆盖表」写在同一次提交里，
于是没有任何产物证明它执行过；同一个缺陷因此在 #84 修复后 35 小时又以 #94 复发（#84、#94）。

机械核对抓的是**被点名**的条目。关键帧只描述不点名（“a boy in a hooded jacket”）时它不发声——那部分仍由
`SHT-22` 的必写字段、`SHT-24` 和审查 rubric 承担。两个如实记录的出口：某个条目被正文提到却确实不在画面里，
在「视觉依据」末尾写 `；画外：<类别>「<名称>」`；某个名字在正文里根本不可靠（`道具「手机」` 撞上「手机店」），
在《视觉设定.md》该条目写 `画面代称：无`。

*升级*：既有《分镜.md》每镜补一行
`- 视觉依据：《视觉设定.md》·人物「<名称>」（控制：<范围>）；……`，条目名称与《视觉设定.md》标题一致。
《视觉设定.md》的条目标题同时收紧为 `## <人物|造型|地点|道具> · <名称>`（分节词、空格、中点都要对上），
分组标题和 `## 角色 · X` 这类写法会被指名报错。提示词正文语言不是中文时，被某镜「视觉依据」引用过的
**人物**条目还要补 `- 画面代称：<正文里的拼写>`，正文从不点名它就写 `- 画面代称：无`；两者都没有时报错，
不再静默跳过。中文项目不需要这一行。

**真实参考图必须声明用途。** `REF-...` 槽位新增 `用途` 字段，取值只能是
`身份`、`造型状态`、`地理`、`构图`、`尺度`、`效果`、`起始帧`、`结束帧`、`风格` 之一；同一条目最多一张
`起始帧` 与一张 `结束帧`，写 `结束帧` 时必须同时有 `起始帧`。《参考角色》一直要求每张图回答「本次
参考身份、造型状态、地理还是起止帧」，但语法里没有这个落点，用途只能靠中文名猜。有了它，视频提示词
能直接回答「本镜送哪一张分镜起始帧、哪些人物/道具/场景图」，参考方式（首帧 / 首尾帧 / 多槽参考）
也从用途组合读出，由命中的模型方言翻成供应商 role（#92）。生产 job 的 `role` 仍是供应商侧的翻译，
`prepare` 不拿它和 `用途` 对账。

*升级*：既有《分镜.md》《视频提示词.md》《图片提示词.md》里的 `REF-...` 在中文名称后的括号里、
`控制：` 之前插入 `用途：<用途>；`，即
`《<中文名称>》（用途：<用途>；控制：……；不得控制：……）`。缺 `用途` 的槽位会得到指名道姓的诊断，
不再报成 REF 语法错误。同一张图现在可以占两个用途不同的槽位；只有路径和用途都相同才算重复。
已经 prepare 落盘的生产 job 不受影响。

**未手工指定参考图不再自动降级为文生视频。** 分镜先按本镜的起始构图、可见人物、
地点与关键道具检查项目已有真实图片；内容和用途可核对的图会自动写入 `REF-...` 绑定。
仍缺图时逐镜写「待补参考图」清单，部分命中则保留已有 `REF-...`，并停在最终视频提示词之前；只有创作者明确选择文生视频才允许无图续跑。
机械校验同步拒绝普通「无」或「待补」与最终文生视频提示词并存，避免后续修订又恢复静默降级（#92）。

*升级*：既有《分镜.md》/《视频提示词.md》若确实要继续文生视频，将两处「输入参考图：无」
同步记为「无（创作者已明确选择文生视频）」；否则先完成参考发现与缺口列表。

### Fixed

**视频生成实际跑不通。** 两处都让 `provider_adapters.py` 的视频路径无法完成一次真实生成（#95）：

- MiniMax 的轮询打在 `{base}/video_generation/{task_id}`，而官方查询接口是
  `{base}/query/video_generation/{task_id}`。任务能创建、能生成，就是取不回结果。
- 两个视频 provider 都拒绝本地参考图，理由写的是「公开契约没有证明本地 data URL 可用」。
  官方文档恰恰把 `data:<mime>;base64,<...>` 与公开 URL 并列为受支持的输入，所以这条理由不成立——
  而同一个文件里的 GPT Image adapter 早就在用 multipart 上传本地字节。现在两个 provider 都把
  确认过的项目相对参考图读成 data URI 直接送出：绑好图就能跑，不需要自建上传服务。
  每个文件的字节必须与扩展名声明的媒体类型一致，并受 30MB/图、50MB/视频、15MB/音频、
  64MB/请求（按 base64 展开后计）的上限约束，超限时指名报错而不是静默截断。
  每张图的 provider role 取自已确认 job 的 `reference_bindings[].role`——创作文档里 `用途` 的
  生产侧译名；带参考图却没有绑定的 job 直接失败，不猜 role。

**「待补参考图」用 `；` 分隔时报错报到了别处。** 缺口清单里出现 `；` 会被当成第四个 REF 槽位，
诊断说「REF 语法不完整」，而它最直接的修法是删掉缺口那一段——正好恢复 #92 报告的静默降级。
现在诊断直接说明缺口之间只用 `、` 分隔。

**参考发现指向了不存在的目录。** 分镜技能原先写「项目「制作成果」」，但项目布局里没有这一层顶级
目录；改为真实的 `剧集/<EP>/制作成果/`。

**创作台把一条提示词画成了好几个框。** Markdown 里连续的 `>` 行是同一个引用块，而 Dashboard 的
渲染器给每一行都单独建了一个 `<blockquote>`，加上样式里的边框和圆角，一条 MiniMax H3 的六段参考正文
就显示成六个独立的框。创作者因此不知道该复制哪一段（#97）。现在连续的 `>` 行合并成一个块，
空行仍然按 Markdown 的规则分隔两个引用块。方言文件和《五份创作文档》也写明：`### 可复制提示词`
下面是**一条**正文，整段复制进那一次生成。

## [0.6.3] - 2026-09-01

本版不改变项目目录，也不要求迁移已有文档。选定目标视频模型后，分镜、视频提示词与生产 adapter
使用同一份版本、正文语言、原生时长、参考方式和声音契约；未选定目标模型的项目继续走通用路径。

### Added

**版本化视频提示词方言。** 新增 Seedance 2.0、Seedance 2.5 与 MiniMax H3 的独立写法，分别处理
各版本的分段结构、素材角色、同轨声音、原生时长和请求参数。生产 adapter 使用同一模型档案编译，
不再让创作文档与最终请求体采用两套参考语义。

### Changed

**视频正文语言和连续性路由由已接受的模型档案决定。** 视频提示词优先使用模型档案声明的语言，
再回退到项目提示词语言；没有项目配置和目标模型的独立任务沿用用户当前语言，不再固定为英文。
同一连续时空内的相邻正式片段，在目标模型支持时默认等待上一段实际视频及其实际尾帧后续接；
一镜一生成只表示打包粒度，不能把连续片段降级为互不相关的文生视频任务。

*升级*：依赖旧版隐式英文默认的独立调用需要显式选择 `en`。既有项目不需要迁移；没有接受目标
模型档案时仍使用通用提示词和原有项目语言回退。

### Fixed

**分镜可见资产引用与视频执行边界。** 冻结关键帧成稿后，分镜按实际画面反查需要保持身份、造型或
地理的可见人物、地点和道具，确保每项由现有 `IMG-...` 或《视觉设定.md》覆盖；同一请求修改图片
提示词和分镜时当轮对齐，不增加字段、迁移或校验门禁。限制动作空间的持续关系从资产状态进入分镜
起终边界，并在运动正文中只写正向状态与允许动作通道。同轨声音以带说话人、口语语言和逐字边界的
完整事件时间线闭合，事件间回到声明的环境声或静默；动作正文不靠否定项描述，声音只集中写上游
已确认的排除，不自行扩张禁令。首次初始化
直接携带输入中已确认的说明语言、提示词语言与画幅，不再先落默认值后改写（#84）。

**视频字幕与画内文字分层。** 除非剧本或创作者明确要求，视频提示词默认关闭非画内字幕、caption
和对白文字叠加；画内精确文字仍按准确字符、语言、承载面和位置单独保留，不被全局禁字误删
（#87）。

**分镜状态、相对时点和对白边界闭合。** 每镜明确写成“起点 → 唯一动作 → 终点”，交接、持物与
身体—物件—空间关系在镜内完成状态转换；“后半段”等相对时点先按已接受集长换算，避免揭示提前。
同轨对白把口语语言、逐字内容、允许事件和已确认的外语前导、额外人声与重复排除带入最终提示词，
后续连续段在实际前段素材可用前保持不可提交。项目初始化同时保留已确认的集数与单集目标时长
（#89、#90）。

## [0.6.2] - 2026-08-30

本版无需迁移，既有项目和目录结构不变。三项能力来自创作者实跑反馈（#81）：跨镜的服装与道具
颜色可以锁住、本地创作台可以常驻、跑完的项目有了一条导出命令。`Changed` 里的两条收紧都是
opt-in——不声明连续性锁的项目行为与 0.6.1 相同。

### Added

**`project_tool.py export`**：把每集现有的五份创作文档与 `剧集/<EP>/制作成果/` 复制成一份交付
目录，附 `manifest.json` 与 `checksums.sha256`。支持 `--episode`（可重复）、`--no-media` 与
`--overwrite`。此前创作者跑完全流程后没有可用的导出命令：`package` 要求每个文件都走过
publish → review → accept，creator-first 项目并不产生这些记录。

这是**当前状态快照**，manifest 的 `asserts_approval` 恒为 `false`，不声称任何审查或创作者接受；
带审批证据的正式交付包仍然只有 `package` / `verify`。manifest 如实记录本次的选择与遗漏：
`excluded` 列出本项目里**实际**没有带走的每个根目录（包括已发布的 `设定集/`），`selection` 记录
是否整集导出、是否带媒体，`missing_documents` 记录哪几集还缺哪份文档。

导出会整体替换目标目录，所以目标的安全性是硬约束：`--out` 不能是项目本身、项目的任何上级目录，
也不能是同一个目录的另一种拼法（大小写不同或 Unicode 规范化不同的同一目录按 inode 判定）；
`--overwrite` 只接受一个由本命令生成过的目录（有 `kind: creator_export` 的 manifest），不会
清空任意路径。无法写进 `checksums.sha256` 的文件名（含换行）、符号链接的文档、剧集目录或
`制作成果/` 一律 fail closed 且不留半个导出目录；在 Windows 上无法解包的文件名记进
`windows_unsafe_paths` 提示，但不阻断。

**Dashboard 可以脱离启动它的 shell 常驻**。`dashboard_server.py` 新增 `--detach`、`--status`、
`--stop` 与 `--restart`。此前服务进程是启动它的 shell 的子进程，会话结束就一起没了，创作者要反复
重开、每次拿到一个新链接。现在 `--detach` 让它在自己的进程组里运行，运行中的地址、端口与 pid
记录在 `<workspace>/.short-drama/dashboard.json`（0600），日志在同目录 `dashboard.log`；
`--status` 重新打印当前链接，`--stop` 停止并清除记录。同一 workspace 已有在跑的实例时，再次启动
只打印同一个链接，不再开第二个端口；并发启动时也只会有一个服务，其余调用打印同一个链接。
是否在跑由一把服务锁回答，不靠探测端口——Dashboard 仍然不含任何对外网络客户端。

常驻带来的三个边界一并处理：workspace 目录消失时服务自行退出，不会留下一个还在监听、还在按旧
链接提供已删除文件的进程；会话记录写不进去（例如只读 workspace）时照常提供服务，只在 stderr
说明 `--status`/`--stop` 找不到它——提供服务从不依赖这份记录；一份记录只对它自己的 workspace
生效，共用 `--session-file` 的另一个 workspace 不会拿到指向别人项目的链接。不写这些参数时，
前台行为与 0.6.1 相同。

**目标模型能力档案**。`$short-drama-video-prompts` 新增一份参考资料，把“按目标模型确认”落成六条
有限的能力轴：单次原生时长、参考条件方式、声音是否与画面同轨生成、画幅与分辨率、正文长度上限、
运镜词汇。取值由创作者声明，套件不内置任何模型清单或供应商名称；未声明时按自足的中性写法写，
未声明不作为审查阻断理由。

**MiniMax H3 视频 adapter**。`$short-drama-produce` 增加第四个可选 stdlib adapter
（`minimax-h3`）：模型 ID、分辨率集合与时长区间必须由账号显式配置，提示词进 `content` 的 text 项
并按 7000 字符上限拒绝，参考图按显式 `first_frame` / `last_frame` / `reference_*` role 绑定，
文生视频必须给出具体画幅（拒绝 `adaptive`），本地参考在没有可信上传时 fail closed。

### Changed

**跨镜一致性锁**（`CON-07` 资产 / 分镜 / 视频提示词，`IMG-13` 图片提示词，均为
`structural_invariant`）。`视觉设定.md` 现在可以给一个可见事实写一条 `连续性锁`，把跨镜不变的
那一小段字（颜色、材质、形制）连同生效镜头一起固定下来：

```markdown
- 连续性锁：LOCK-KNIT《织了一半的毛衣》（镜头：全集；图片提示词项：IMG-PROP-KNIT）· 锁面：pale blue chunky knit wool sweater
```

生效范围内的冻结关键帧、`MOTION-...` 正文和被点名的 `IMG-...` 正文都必须含有这段锁面，
`creator_markdown_check.py` 会机械核对。此前只有给创作者读的「识别锚点」，执行端读不到它：
视觉设定写了毛衣是浅蓝色，第一镜生成蓝色、第二镜生成红色，而每一条提示词单看都成立，所以文字
审查抓不到。图生视频“参考帧已经交代过就删掉重复描述”是常规做法，锁面是它写明的例外。

核对规则按「这条提示词是否真的把这个事实写进了画面」来定：忽略大小写，硬折行按空格处理；粘在
词上的匹配不算（`chipped white enamel mug` 不被 `unchipped white enamel mug` 满足）；写在
`no`/`without`/`不要`/`避免` 之后的匹配不算，那说的是不要出现它。列表记号与缩进不影响识别，
而一条看起来像锁却写不完整的行会被报成语法错误并指出原文，不会被悄悄跳过——锁不能变成空转。

写法、什么值得上锁与反例见
[跨镜一致性锁](skills/short-drama-assets/references/continuity-lock.md)。

*升级*：没有声明任何锁的项目行为不变，既有产物不受影响。声明一条锁后，生效范围内缺锁面的
关键帧或运动正文会被校验器指出来——那正是它要抓的漂移。

**`VID-23`**（`reviewed_invariant`）。目标执行端与画面同轨生成声音时，本镜不要的声音必须写进
可复制正文——没写出来的通道不会安静，会被执行端用自己发明的配乐和台词填满，和没写终点的动作
会被自己发明的动作填满是同一件事。执行端不同轨或未声明时，这一条不适用。

### Fixed

**Windows 上的参考图不再被截断**。生产环节按描述符读参考图时缺 `O_BINARY`，Windows 因此按文本
模式打开：读到第一个 `0x1A` 就停，`\r\n` 也被折成 `\n`。PNG 签名的第七个字节正是 `0x1A`，
所以参考图以 5 字节发给供应商、换回 `invalid_image_file`（HTTP 400）——`references` 与
`reference_bindings` 在这个平台上从未成功过。v0.6.1 给 Dashboard 做过同一处修复，生产环节漏掉了。
POSIX 行为不变。

**Windows 上的校验脚本不再把做完的活报成失败**。十二处命令行输出用 `ensure_ascii=False` 打印
JSON，而输出里带着中文路径与中文条目。stdout 重定向到文件或管道时，Windows 用 ANSI 代码页而不是
UTF-8：产物已经写完落盘，进程却在打印那一步抛 `UnicodeEncodeError`，退出码从 0 变成 1，
`screenplay_index.py` 的 `--fail-on-review` 判定也不再执行。命令行 JSON 现在一律 ASCII 转义，
与 `adapter-contract.md` 早已写明的可移植规则一致；写进文件的 JSON 与 JSONL 仍是原样的 UTF-8，
创作者打开产物看到的中文不变。

`creator_markdown_check.py` 不打印 JSON，直接写中文诊断与剧集路径，同一个重定向下也会抛错——
一份完全合格的剧集退出码从 0 变成 1，不合格的剧集只剩一段 traceback，看不到哪里不对。它的 stdout
现在与 stderr 用同一个 `backslashreplace` 处理器：终端能显示中文时照常显示，不能编码时退成转义，
不再中断。

## [0.6.1] - 2026-08-25

维护版本。解除两处挡住创作者的限制——Dashboard 在 Windows 上无法启动，图片提示词校验器用固定
质量词表阻断交付——并收紧生产环节的引用契约。文档结构与 0.6.0 相同，项目目录无需迁移。

**补丁号不传达这一点，故在此明说**：本版含一处会阻断既有产物的收紧，见「变更」。

### 变更

**已确认生产 job 必须点名 canonical 创作文档里的条目**。`prepare` 现在要求 `source_entry` 指向
`图片提示词.md` 或 `视频提示词.md` 中对应的 `IMG-*` / `MOTION-*` 标题，并核对该标题下的可复制
提示词与参考图槽位、顺序、路径、名称和控制边界；任一漂移都 fail closed。存在真实参考图时需逐张
填写 `reference_bindings`。分镜与视频提示词逐镜携带相同的 `REF-...` 语义；`分镜.md` 的「输入
参考图」路径不再被当作生产输入快照。

*升级*：0.6.0 时代的生产 job 若缺 `source_entry`，或正文与 canonical 文档已漂移，`prepare` 会
拒绝。补上 `source_entry` 并让两处正文一致即可，创作内容无需重写。可先用
`skills/short-drama/scripts/creator_markdown_check.py` 核对五份文档的跨文档结构。

### 新增

**Dashboard 支持 Windows 原生环境**。此前在入口即失败（`openat` 不可用）。目录访问改为双后端：
POSIX 仍按描述符钉住，其余平台按逐层校验的路径钉住，拒绝链接与 reparse point，并在打开前后比对
`st_dev`/`st_ino`。配套修复文本读写改二进制模式、跳过无此语义平台的 `fchmod` 与目录 fsync、
文件被占用时给出准确报错。

**原著自带的真实品牌与现实实体有了决策点**。可读文字或标识指向现实存在的品牌、商标、机构或人物
时，四个文字政策选哪一个是创作者的决定：资产环节按未决决定记录（`decision_kind: unresolved` +
`creator_question` + `downstream_impact`），`text_policy.mode` 在选定前不改写，也不擅自改名。
`craft_default`，不判断合规、不内置清单。

**五份创作文档齐备后可做跨文档核对**。各阶段技能的「完成」条目现在指向 `$short-drama` 的
`creator_markdown_check.py`。

**仓库迁移到 `zenstory-ai` 命名空间**。GitHub 自动重定向，既有 clone 无需修改 remote。

### 修复

**图片提示词校验器不再用固定质量词表阻断交付**。`8k|4k|uhd` 与 `masterpiece|best quality|
ultra-detailed|trending on artstation` 命中即返回非零，但其执行的 `IMG-02` 是 `craft_default`，
跨两级阻断。引擎 flag 与权重语法等结构检查保留。写法是否空泛改由审查阶段按证据处理。

**分镜层不再默认用空的建立镜头开场**。原文把建立镜头写作「常见选择」，与剧本环节「在压力活跃处
进入」的默认相反。改为地理在动作内部到达；交代空间的镜头需承担本镜因果，或后续方位判断依赖它。
`craft_default`，项目声明不同观看契约时可覆盖。

**短剧写作的按需路由指向具体小节**。一条路由带四个触发条件指向同一份 745 行文件，四个触发词现在
各自链到对应小节；`screenplay-format.md` 补上「规范化现成剧本」触发词，此前该路径不可达。

**真实参考图的语义绑定贯通到生产**。无真实图片的视频提示词必须把静态视觉锚点写入可复制正文；
内置 adapter 把已确认的引用语义传给供应商，而不是只上传无标签文件。

**Dashboard 阅读与编辑体验修复**。长文档可直接滚动；阅读/编辑切换按相对位置同步滚动，打开新文件
从顶部开始；超长项目名在桌面与 390px 窄屏都不撑出横向滚动；空项目明确结束忙碌态。CI 新增真实
Chromium 交互回归。

**分镜不再把图片提示词 ID 冒充为已有图片参考**。

**悬空符号链接不再能被写穿**。

**保存时拿不到项目锁不再报成内部错误**。

## [0.6.0] - 2026-08-23

本版把默认创作路径从结构化流水线改为 creator-first 五文档，并重整短剧写作规则的
启用方式：工具与审查只在题面确实承诺某种叙事机制时应用对应约束，详细 knowhow 仍按
具体问题提供。版本同时补充漫剧关键帧词表与端到端创作指引，并让 Dashboard 原生识别
新的文档布局。

这是一次**破坏性版本升级**。creator-first 不是旧结构化项目的就地升级格式；旧、新两套
创作真相不得在同一项目根目录并排维护。

### 变更

**公开创作流程改为 creator-first 五文档**。普通创作只按需维护 `剧本.md`、`视觉设定.md`、
`分镜.md`、`图片提示词.md` 和 `视频提示词.md`；场次/资产/镜头批次自动续跑，不再为每批生成
JSON/JSONL、索引、指纹、覆盖表或 QA 记录。用户点名整集或整阶段时一次完成，也可从任一创作
阶段直接进入，不为流程完整补造名义上游。审查结果改为创作者可读的 Markdown；外部媒体生产仍须
经过准备、显式确认和执行三个阶段。

**Dashboard 原生识别 creator-first 五文档**。中文文件名会进入正确内容分组，默认打开 `剧本.md`，
并从可见文档和媒体推导本集进度。

**短剧写作专项规则改为按机制启用**。SCR-13（艰难选择）、SCR-14（争议证据）、SCR-16
（字面精确死线）与 SCR-17（有限单位完成边界）只在剧本实际承诺对应机制时审查；事务性角色、
安静收束、蒙太奇或主观时间，以及故意保留的悬疑，不再被迫改成同一套模板。顶层 Skill 改为
输入优先的规则选择器，详细 knowhow 完整保留在按需参考中，并补充不发明精确数字、记录、资源
与程序，证据验证停止条件，以及连续性标签不能证明镜头外负面事实等边界。

**内容质量门禁扩为跨题材、可复现的隔离 A/B 协议**。公开 corpus 区分可迭代的 development、
冻结后只运行一次的 holdout 与负对照；每个案例、每个 arm 固定执行 3 次独立创作，再由两个
模型家族进行 A/B 换位盲评。生成端只接收各自的写作 Skill bundle，评审端只接收本次 prompt；
收据分别绑定 `source-bundle-only` 与 `prompt-only` 策略。schema v5 按 replicate、案例逐级
等权聚合，并通过仓库外的维护者可信 seal 绑定 corpus、源码 bundle、基线提交、中性提示词、
量表、模型配置、私有泄漏词表与调用收据。

**贡献测试不得用纯字符串包含断言钉死文案**。规则契约应结构化验证，工具用输入/输出夹具证明
行为，内容判断使用隔离实跑与盲评；泄漏扫描器的测试验证扫描边界，不把当前文档的一句话当作
成功条件。

### 迁移与回滚

- **v0.5 在途项目继续锁定 `v0.5.0`**。不要在原项目根目录直接切换到 creator-first，
  也不要把两套产物并排放置后交替调用不同版本。
- **升级时新建 creator-first 项目根目录**。逐集人工转移并重新确认 `剧本.md`、
  `视觉设定.md`、`分镜.md`、`图片提示词.md` 和 `视频提示词.md` 这五份创作真相；不要把
  旧 JSON/JSONL 自动转换后直接视为已确认内容。
- **旧结构化产物、生产记录与审计证据只读保留**。它们用于追溯既有生产，不再作为新项目
  的并行真相来源。需要回滚时，回到原 v0.5 项目根目录并继续锁定 `v0.5.0`；不要把新五文档
  反向写回旧结构化目录。

### 新增

**`short-drama-storyboard` 新增漫剧关键帧视觉词表**（`taste_option`）。
`references/comic-keyframe-lexicon.md` 把漫剧 / 二维漫画形态下的项目级共享画风与镜头级
可读性约束整理成"场景/主体事实 → 视觉语言投影 → 可读性/风险约束"三层结构，并给出
叙事关键帧、对话镜头、动作特效帧的选词入口、组合方法与完整示例。选项本身不设质量门槛，
被创作者接受进视觉方向后才成为必须投影的形态约束；身份、地理与边界事实的优先级不变。

**docs 新增《漫剧 Creator-first 全流程》**。`docs/comic-drama-workflow.md` 把十个技能串成一条
可照做的漫剧产线路径：从初始化与视觉方向，到剧本、资产、图片提示词、分镜关键帧、视频
提示词、确认后生产、审查与文本交付，含每步命令、产物位置与常见卡点。

### 修复

**修正随发布 Markdown 中无法闭合的粗体标记**。将粗体段落末尾标点移到标记之外，避免
句末标点紧邻闭合标记的写法在 GitHub 按字面显示星号；新增 CommonMark 形状检查覆盖
随发布文档，固定评估产物保持原样。

## [0.5.0] - 2026-08-19

本版来自一次覆盖全仓库的复核，收紧了多处结构约束，并把若干条只写在文档里、
脚本并未实现的行为补齐。因约束收紧，既有产物可能需要补字段后才能再次通过
检查，逐条见**变更**。

### 变更

**`duration_estimate.py` 必须传 `--index`**。台词与动作段的判定改为全部取自
`screenplay-index.jsonl`。此前脚本自带一套剧本解析：`[VO]` 计零秒、Markdown
注释计入台词字数、带冒号的动作段被读成对白、多行动作段按行数重复计算。
索引与剧本字节对不上时拒绝估算；索引中仍有未归类文本时，报告给出 `incomplete`
段落说明本次估算未覆盖全篇。参考运行 EP001 仍为 94.8 秒。

**覆盖表与镜头文件对账，四种处理均可表达**。处理表现在逐条校验：状态限于
工作流定义的四种、一个块只能有一行、点名的块须在剧本索引内、`intentional_repeat`
与 `omitted_with_reason` 须写理由。认领检查按处理判定，`nonvisual_context`
与 `omitted_with_reason` 不再要求镜头认领。新增 `shots.jsonl` 与时长总和的
对账，以及同一镜头在总和中重复出现的检查。

**交付容器补齐四个结构校验点**。`delivery-container.jsonl.md` 列出的七项中，
成员 `order` 序列、成员 `accepted_duration` 与所装镜头一致、`membership_basis`
三项结论完整，现由脚本判定。既有容器需补 `membership_basis`。

**开放的 fatal / error 结论必须写 `required_change`**。此前该字段仅对
`targeted_edit`、`resubmit`、`rewrite` 三种处置强制，而 REV-11 将项目校准
之外的结论路由至 `not_applicable`，使阻断交付的结论可以不写修复方向。

**配音本 `channel` 与其投影的块对账**。`[VO]` / `[OS]` 块只能记为同名声道或
`dubbed`，画内对白块不能记为 VO / OS。

**章节单位按层级选取**。改为取能构成编号序列的最粗单位，并新增 `--unit`
指定。此前取出现次数最多的单位，而子节数量恒多于章，「第N章下分第N节」的
书因此被按子节切分。选中较细单位却跳过更粗单位时，写入 `problems`。

**`SHT21_*` 诊断码改名为 `SHT01_*`**。这组代码实现的是 SHT-01（每个制作
相关的块都有覆盖处理）；SHT-21 是关于关键帧交付文案的 reviewed_invariant。

**图片提示词按来源政策判定呈现方法**。`common-recipe.md` 的映射表现由脚本
执行，越表的修复方式是创作者改政策。`IMG-07` 的判据由两个字面量扩为覆盖
常见写法。

**三处检查项改名为 `source_declaration` / `evidence_declaration`**。这些检查
确认引用的 `src` 在本文件 `sources` 中有对应条目；跨文件的引用完整性由仓库层
测试判定。原名易被读作「引用已解析」。

**剧本索引 meta 新增 `block_id_high_water`**。旧索引缺该字段时按空表处理。

**创作台按产出阶段分组**。`project_tool status` 新增 `ownership`（路径 →
拥有它的技能）。路径规则仍然主事，拥有者兜底路径规则认不出的产物，新增阶段
的产物会落进自己的组。

**原著分析自成一组，默认折叠**。实测参考运行：全剧组由 43 个文件降至 14 个，
其余 29 个进入折叠分组。搜索时照常展开。

### 修复

**块 ID 退役改为永久**。此前编号仅从上一版索引推导，退役只有一代深：某场景
中某一类块全部消失后，下一版从 01 重新发号，把已退役的编号发给新内容。下游
产物裸引这些 ID，旧引用仍可解析，指向的却是另一段内容。索引 meta 的
`block_id_high_water` 逐版传递每个场景、每种块用过的最大编号。

**接受态产物的字节校验补上测试，两处实现合并为一处**。一份 accepted 产物
被绕开工具修改后须退回 `update_needed`。同一套规则此前分别实现两次且无测试
断言两者一致，创作台渲染的是其中一份。

**上游投影不再优先于其来源**。运动规格的 `duration_ref.record_id` 无法解析
时不再回退到规格自报的 `value_seconds`；容器成员的 `accepted_duration`
开始与镜头比对。「投影是否过期」的判定移至时长模式判断之前——参考运行的
二十条运动规格均为 `relative`，该守卫此前在真实产物上不会运行。

**可复制提示词正文跟随 `prompt_language`**。三处随技能发布的模板此前把正文
语言写死为中文，与 SKILL.md 声明的契约冲突。参考运行的七条正文改写为英文；
画面内可读文字由文字政策决定，与描述语言无关，因此保留。

**随发布模板的悬空指针检查扩至全部产物**。此前仅覆盖一个写死文件名的产物，
另有十八条指针不被检查。现按产物查找模式、两个真相来源取并集、并走完整条
指针（此前只核对第一段）。覆盖由 14 条升至 32 条。

**不带上一版重建索引时命令停下并指名该文件**。照此前文档所写重跑同一条命令，
44 个块会按位置重新编号，被改写的块拿回前一个块退役的编号，下游引用解析得开
而内容已换。确需重编号时加 `--no-previous`。

**`[VO]` / `[OS]` 计为口播**。时长估算与配音本校验此前都不承认这两个标签：
一集把内心戏交给画外音的剧本被报为 64.0 秒（真实 94.8 秒），配音本装不下
18 句中的 8 句而 `uncovered_dialogue_blocks` 报空。

**随技能发布的模板不再带悬空指针**。`episode-card.json` 投影了 5 个分集地图
已删除的字段，`beats.jsonl` 的 `payoff_refs` 默认指向已删字段。

**引用完整性测试开始识别 `.json` 产物**。`declared_sources()` 此前只认独立的
`record_type: sources` 记录，紧凑写法的 `.json` 被整体跳过。

**三处文档表述与实现不符**。`STY-24` 缺表格行；核心 `SKILL.md` 的锚点指向
不存在的标题；样例 README 承诺的 SHA-256 在样例中并不存在。

**`unmapped_spans` 的覆盖范围写准**。它标出第一个集标题之前的材料；集间说明
与尾注因分集跨度首尾相接而无法标出，这属于语义判断。「材料不会静默丢失」由
文档要求的边界检查保证。

**`voice_sheet_check` 遇到落在半个字符上的块跨度时报出该行而非中断**。

**索引未完全归类时，覆盖检查报出来**。一行被标为 `ambiguous_dialogue_or_action`
的文本不会作为块进入索引，因此覆盖、时长与配音本都看不到它。新增
`SHT01_SCREENPLAY_IS_NOT_FULLY_INDEXED`。`short-drama-write/SKILL.md` 同时改正：
该情形的三种处理方式中，「保留为动作」此前写着可行，实际做不到。

**资产接受记录的 `CD-` 前缀写进文档**。`creator_acceptance.decision_ref` 的
`record_id` 必须以 `CD-` 开头，这条规则会拦下发布，而此前只存在于检查器的报错文本里。

**逐章提取规范补上四条下游依赖的规则**。首行标题格式、原文本身是内心独白时怎么写、
`P<n>` 与类型之间是半角空格（下游按字面形状 grep）、`功能：` 是字段标记不能在别处使用。
机械自检同时改正两处：`功能：` 的计数改为只数情节点行第四段——此前数的是全文任何位置，
正文里提一句「功能」就会把一份合规的提取判成失败；载体表的检查改为数数据行，
此前只核对表头存在。

**姓氏＋头衔单列为可合并的别名类**。「周团」「钟记者」在中文叙事里常是最主要的称呼，
按「头衔职务」一律禁止合并会让一部书留下十几条重复实体。判据是去掉头衔后还剩不剩
姓名——不带姓名的泛指（「护卫队长」「家主」）仍不可合并。

**`归属置信` 补上分子的定义**。此前只有公式没有「有明确归属」的判据，按连续章节切分时
该比值恒等于 1。

**S1 抽样与 S5 全量的三类判定比例改为按方向对照**。前者判章，后者判剧情单元，两者
之间没有换算；按章数加权回推只会得到一个看起来精确的假数字。

**并发子代理与发布路径的分工写清**。子代理落 `_work/chapters/`，主线程发布到
`chapters/`；覆盖率闸门按正式路径匹配，发布之前跑会让每一章都进 `unmatched_files`。
`_progress.md` 每阶段重写而发布要求一个路径一个 owner，固定 artifact-id 为
`source-analysis:progress`。

**主工作流实跑必须固定在一个提交上**。实跑要走几个小时，其间套件文件变化会让报出来的
摩擦无法归属到某一版。`evaluations/README.md` 与 `CONTRIBUTING.md` 给出 `git worktree`
的做法，并要求把提交号写进报告。

### 新增

**主工作流评估：`evaluations/让你管账号/`**。一部完整长篇（20 章 / 147 KB）
作为固定输入，外加按文档实跑一次录下的全部产物，21 个产物均为 `accepted`。
`tests/test_workflow_evaluation.py`（10 条）作为回归闸门，已列入
`CONTRIBUTING.md` 的发布前必跑。人工实跑的做法见 `evaluations/README.md`。

**`tests/test_guards_bite.py`**。覆盖九处此前无测试保护的守卫，每条均按对应
的源码改动验证过会失败。

**诊断码目录完整性测试**。检查器能报出的代码，其所属技能的参考资料中必须
查得到。据此补齐 27 个代码：11 个 `SHT01_*`、4 个 `VID15_*`，以及配音本的
12 个 `VOICE_*`（此前无目录）。

**链接锚点纳入校验**。此前 `#` 之后的部分在检查前被丢弃。

**评估闸门新增两条断言**。参考运行的时长断言为具体数值；七条提示词正文除
画面内可读文字外不得含项目未声明的语言。

**`STY-24 · reviewed_invariant`：转折写成那一下动作，不是那一下的功能**。
分集记录中每一集的方向性转折与当集兑现，各自要能指出一个具体的可见动作
（谁做了什么）；反对力量的筹码同样写成已发生或将发生的行为。判据是「这一拍，
观众看见的是谁做了什么」。不用关键词筛，也不要求写成完整场次。

### 已知缺口

- `unmapped_spans` 不标集间说明与尾注；材料完整性由文档要求的边界检查保证。
- `block_id_high_water` 不保护该字段引入之前退役的编号。
- 跨文件的 `field` pointer 只由 `tests/test_reference_integrity.py` 判定，
  各技能检查器只能解析命令行传入的文件。
- `short-drama-review` 与 `short-drama-produce` 不在主工作流评估闸门覆盖
  范围内：前者是单独请求的审查阶段，后者需要本机没有的生成后端。

## [0.4.2] - 2026-08-18

v0.4.2 是一次校准版本，有两个来源。一是和竞品的端到端对比实跑：同一部小说、同一个目标，
两套 pipeline 各走一遍从原著到 EP001 视频提示词的全链——下面前两条改动来自它。二是对产物内
哈希的一次全仓清点，来自第三条。两者的共同结论是同一句：实跑一遍比补一屏断言值钱得多。

**提示词正文不再是占位符的形状**。实跑里 28 条关键帧提示词是同一句模板复制 28 遍，14 条运动
提示词把约束清单复述了一遍——都读起来很规范，都没有一个可以画出来的名词。根因是随技能发布的
模板：关键帧占位符写的是一句约束，运动模板写的是一句抽象描述，而唯一把真实正文放进示例的
图片提示词，正是唯一输出合格的一路。三处占位符改成描述画面本身，两个阶段的 SKILL.md 指向
各自已有的成稿范例，并把「交付文本只含要拍出来的画面内容」这条一直是硬规则、却从没有编号的
约束登记为 `VID-22`。

**产物里不再有任何哈希**。全仓清点发现这些字节摘要全部是人手工填、没有任何脚本回头核对的，
于是每次上游编辑都要重算整条下游链，而漏算不会报错——样例里 331 个此类哈希已经全部与字节对不上，
其中一次同类重构漏掉一个键，静默烂了好几个版本。本版本把产物层的哈希全部删干。
实跑逐个确认了代价：**真正失去的只有一项**——剧本索引按内容保 block ID。原著漂移检测退化
（行数或编号变了照样报，同行数的原地改写不再报）；配音本漂移仍然被逐字比对抓住，丢的只是
「索引过期」这个单独诊断码；整稿导入的 span 校验基本仍在。
工具自己维护的那一层不变：生命周期状态、交付包校验和、生产任务指纹。

**必需输入有了合法的写入路径**。`duration_estimate.py` 必须读 `format.pacing`，而
`short-drama.json` 不能 publish、`set-authority` 又不收这个字段——唯一的办法是手工编辑，
而那是这套契约唯一明令禁止的动作，整条链路没有一处发现。现在它进了白名单，`status` 也会
把手工编辑报出来。

此外：Python 下限降到 3.9，分集记录按「删掉它会不会失去创作约束」重新裁剪，
新增机制循环、破镜重圆题材卡与若干剧作写法。

### 新增

- **补上引擎与分集之间的那一层：机制循环**。新增
  `short-drama-develop/references/mechanism-loop.md`，回答"同一件事凭什么值得再看一次"：
  怎样写清一次运行、怎样区分一次运行与一次重复、循环耗尽的症状与根因（机制越有效，
  对手越必须变笨），以及收束、改造、移交三种处置和不成立的第四种（不处置旧循环就并接
  新循环）。同时写清换循环时最容易丢的是紧迫性而非新鲜感，以及没有具体去处的循环必然
  耗尽。`STY-20` / `STY-21` 为 `craft_default`，不规定运行次数、循环长度或换代位置。
- **发现重复之后的两个修法，只有一个是对的**。同一文件写明：换靶心会让观众追的线断掉，
  正解是换向量、抬阈值、不换靶心；判据不是"有没有换人换事"，而是"换过之后观众追的
  还是不是同一件事"。
- **循环之间的交接与嵌套**。`mechanism-loop.md` 增加做法层：在当前循环还在运行时，就让
  下一个循环需要的东西以次要身份出现过一次，换代时观众感到的是"原来那个有用"而不是
  "怎么突然又来一件事"；并说明循环通常是嵌套而非排成一列——大循环的期待要更早建立，
  换代之间需要一段交付余波的缓冲。不给层数与集数配方。
- **回报词本身不构成承诺**。`story-craft.md` 增加落点一节：同一个回报名在不同观众承诺下
  可以落在关系、地位或认知上，落点决定哪些事件算数；不写清落点的偏差要到中段才暴露。
  同时增加"回报要先有舞台，再有能力"——在主角取得关键优势之前，观众要已经认得具体的人、
  场合或那句话。
- **多人场次先解决"谁是谁"**。`script-craft.md` 增加减人、陆续进场、分段轮换主对手、
  人物之间明确递交四种办法，并写明仪式/围攻/闹剧可以有意违反。新增 `SCR-12`
  （`craft_default`）。
- **主角不在场的场次怎么判断成立**。同一文件写明：镜头可以整场甚至整集不给主角，
  条件是情绪、推进、戏剧性与期待仍挂在主角身上；删掉后主角处境与观众期待都不变的场次，
  多半是在替配角讲他们自己的故事。
- **修订流程增加扫读遍**。`script-craft.md` 第 9 节新增：用远快于正常阅读的速度扫一遍，
  只回答"哪里需要停下来才能想明白""扫完有没有情绪起伏"。慢读证明剧本讲得通，扫读
  才证明它接收得到。它是自检手段，不是质量门槛。
- **写下游内容时怎样定点回原文**。`adaptation-craft.md` 增加一节：用"人物 + 地点/物件 +
  当前动作或冲突"的事件锚点检索，而不是用会跨全书反复命中的概念词；命中后回读原文段落，
  不凭检索摘要写。最容易被忽略的一条是命中可能来自故事后段——那里的知情范围与能力边界
  都已推进过，直接用会让人物提前知道此刻不该知道的事，而且读起来完全通顺、很难在后面
  被发现。判定办法是把片段的知情状态与当前集进入状态逐项对照。新增 `STY-22`
  （`craft_default`）。
- **创作者声明的门槛集**。`episode-design.md` 增加可选结构：项目可以声明一个观众要继续
  看下去必须先付出额外代价的门槛集。门槛不新增规则，只让已有的三条在这个位置收紧——
  出去的压力、兑现依赖已建立的事实（`STY-09`）、每集都有局部结果（`STY-13`）——因为
  别处失手观众只是走神，这里失手观众直接离开。位置、比例与是否设门槛全部由创作者决定；
  套件不提供默认值，不假定任何平台规则，也不把门槛位置当作质量判据。
- **新增破镜重圆题材卡**。`genre-cards/破镜重圆.md`，置信度中。核心是"你在我不在的时候
  还爱不爱我"这一问题的反复证明——分离是证明的条件而非障碍；按误会归属分四支
  （无过失、一方误会、双向误会、一方不知道对方是谁），并写明"此刻不能立刻说清的理由"
  是最容易漏掉也最致命的一件。卡片仍是可被创作者一句话覆盖的 `craft_default`。
- **剧本阶段可以估算时长了**。新增 `short-drama-write/scripts/duration_estimate.py`：按项目
  在 `short-drama.json` 的 `format.pacing` 里自己声明的速率，把台词字数与动作段数折算成秒，
  并与 `target_seconds_per_episode` 比较。套件**不带跨项目速率、不设容差带、不阻断交付**——
  密集争辩和沉默劳作本来就不是同一个折算比。没有声明速率时只报计数，不猜秒数。
  此前时长算术只存在于分镜与运动层，等发现装不下时戏已经写完了。
- **`format.pacing` 有了写入路径**。`set-authority` 的白名单从只收
  `/format/target_seconds_per_episode` 扩到同时收 `/format/pacing`，值按类型校验（速率对象、
  每个速率是正数），取值是否够用仍由写作阶段判断。此前这个字段是 `duration_estimate.py` 的
  **必需输入**，却既不能 publish（`short-drama.json` 是受保护路径）也不能 `set-authority`，
  实跑里只能直接编辑 manifest——而那正是这套契约唯一明令禁止的动作。对象槽位是**并入**而不是
  整体替换，所以校验的是合并之后的结果：一条只写了一半速率的决策会被拒绝，而不是写进去之后
  报 `bound`、同时让时长估算继续不可用。
- **`status` 会报告 manifest 被手工改过**。新增 `authority` 一节，逐个字段对账
  `set-authority` 当初记下的值与 manifest 当前值，报 `bound` / `hand_edited` / `missing` /
  `not_authority_field`。绑定信息本来就存在状态里，只是从来没有人读它。这一节是给 CLI 与
  reviewer 看的，创作台按设计过滤生命周期内部信息，不显示它。
- **分镜覆盖对账剧本，按块 ID**。`storyboard_check.py` 增加 `--screenplay`：剧本里的每一个块
  必须被恰好一个镜头认领。没有镜头认领的块不会被拍，两个镜头认领的块会被剪两次；两者都是
  引用事实，不涉及审美。新增 `SHT21_BLOCK_UNCLAIMED` / `SHT21_BLOCK_CLAIMED_TWICE` /
  `SHT21_BLOCK_NOT_IN_SCREENPLAY`。按块 ID 而不是正文行比对：内容未变的块保留 ID，改写的块
  换新 ID 并让旧引用失效。
- **动作的可生成性**。新增 `short-drama-video-prompts/references/generability.md`：
  **仅当项目声明由生成模型承担画面时生效**。判据是"这个动作在日常影像里常见吗"，
  并给出四类高风险写法（精确拦截、不可见内部状态、否定式动作、三步以上双手编排）
  与五种改写方法。核心一句：戏剧信息来自常见动作的**组合与时机**，不来自动作本身的精巧。
  写成参考资料而不是关键词检查——关键词匹配会把「退伍军人群里」误判成人群场面。
  新增 `VID-21`（`craft_default`）；实拍或形态未声明时不生效。
- **`sources` 的语义写进文档**：它只记录**派生来源**——本文件依赖、上游变了就可能要重做的
  那些产物。同层互相指名不是派生（occurrence 指向它喂给的 decision，那个 decision 指回它
  消费的 occurrence，两者都成立），**只有派生必须无环**。
- **枚举报错列出合法取值**。`disposition`、`purpose` 两处枚举失败与
  `SHT16_DISPOSITION_MISSING` 现在直接写出可用值，`container_duration` 的类型报错改成
  "是秒数，不是文本"。实跑里这几条各花掉一次往返，而合法取值本来就在脚本里。
- **悬空语义引用测试**（`tests/test_reference_integrity.py`）。引用指向的文件存在，不等于
  它指向的**东西**存在：`field` 可以指向记录里没有的键，`projected_fields` 可以指向已被
  删掉的契约字段，`source_step_pointer` 可以指向不存在的因果步。上一轮缩表就留下了八处
  指向 `/causal_escalation/0` 的悬空指针，全部既有测试保持绿色，是靠人读出来的。
  新测试按 JSON pointer 实际解析，并处理一个关键形状：单集卡把契约引用写在
  `contract_authority`、把指针写在 `execution_plan`，两者是兄弟而非祖先——只向下继承
  引用会漏掉的正是这一类。

### 变更

- **交付文本只含要拍出来的画面内容，现在是一条有编号的规则**。新增 `VID-22`
  （`reviewed_invariant`）：镜头与记录 ID、规则 ID、生命周期状态词、哈希、字段路径、参考图
  文件名、工艺备注与成段否定罗列都留在规格字段与审查记录里，交付文本只承接它们的结果。
  这条一直写在 `production-prompt-grammar.md` 里并自称硬规则，却从没有编号、也没有进契约表。
  关键帧那一侧同样的义务登记为 `SHT-21`（`reviewed_invariant`），并写明同一集里两条关键帧的
  正文只差编号就是模板不是画面。两条都按仓库先例写明由 reviewer 引用对照，本套件不做机械判定。
  同一节补上实测最常见的违反形态：把本文件的禁令原文抄进提示词——它读起来很规范，
  但整段没有一个可以画出来的名词。判据是一句话：把这段文本交给一个没读过规格的人，
  他能不能画出一张确定的画。
- **随技能发布的提示词占位符改成描述画面本身**。`keyframe-template.jsonl` 原来写
  `<one frozen instant; no ordered motion>`（一句约束）、`motion-spec.jsonl.md` 原来写
  `<从本规格渲染的可复制通用视频提示词>`（一句抽象描述），照着它们写出来的正文因此也是约束
  和抽象描述。两处改为逐项列出正文该带的画面事实，并指向各自 reference 里已有的成稿范例。
  分镜与视频提示词两个阶段的 SKILL.md 同步写明：`generic_prompt` 与派生文本是同一段交付
  文本，同一集里两条正文读起来一样就说明写的是模板不是画面。
- **提示词正文不得携带引擎专用语法**。`image_prompt_check.py` 的
  `generic_prompt` 检查从只查哈希泄漏扩到同时查权重语法（`::2`、`(x:1.2)`）、命令行式控制词
  （`--ar`、`--seed` 等）与画质词（`8k`、`masterpiece`、`best quality` 等）。这些写法
  `common-recipe.md` 与 `keyframe-craft.md` 早已明令禁止，此前没有任何地方执行。
  引擎相关的写法属于 provider adapter，不属于 provider-neutral 的通用提示词。画质词只在
  逗号分隔的标签位算违规——画面里真有一台 4K 显示器、墙上挂着一幅 masterpiece，那是内容。
- **剧本索引修订时的说话者清单改为并集**。带 `--previous-index` 重建时，上一版已经解析过的
  说话者会被读回，与本次传入的 `--speaker` 取并集。此前只要显式传了任何一个名字，其余对白就会
  退回 `ambiguous_dialogue_or_action` 并换掉自己的 block ID——而"只传新增的人"正是修订时最自然
  的动作。

- **登记不等于兑现**。新增 `STY-23`（`reviewed_invariant`）：简报登记为不可更改的来源事实，
  在本轮覆盖范围内要么有一处写进了分集记录与剧本的可见载体，要么显式标注延后到某一集；
  分集记录声明的可见载体必须在对应剧本里实际出现，需要对照才成立的载体两端都要出现。
  `adaptation-craft.md` 增加 5b 节，创作简报模板的「不可更改的来源事实」改为每条要写清
  它在哪一集、以什么可见方式落地。被自己标为地基的设定写进简报之后再也没有回来，
  是一种每次审查都读到它、观众却一次都没看到的失败。
- **配音本不再是可选项**。项目的对白由录音或 TTS 承担时，配音本与剧本一起交付；
  只做无声或字幕交付时才跳过，并写明理由。音色描述仍属于角色档案，配音本用 `source_ref`
  指向它。此前它写成「需要配音本时（可选）」，实跑整轮下来一份都没产出——
  拿到剧本的人手里有台词，没有任何一句该怎么念。
- **分镜时长先折算再写**。写 `duration_seconds` 之前按 `format.pacing` 把每个镜头认领的剧本块
  折算成秒，与剧本阶段用同一组速率。实跑里凭感觉写完 13 个镜头，到覆盖检查才发现整集超目标
  47%，返工的是全部镜头。
- **分集记录按"删掉它会不会失去创作约束"重新裁剪**。`episode-map` 顶层字段 23 → 17，
  Golden Sample 每集散文槽位 45 → 25、重复率 38% → 16%。删除或下放：`title`、
  `promised_payoff`、`rhythm_plan`（从不进入写作）、`character_progression` 与信息权限模型
  （属于故事引擎，整部戏只写一次）、`active_pressure`（与 `opposition.goal` 同义）、
  单步 `causal_escalation`（完全可由目标/反对/结果/出去压力推导），以及 `hook.kind`、
  `turn` 的两个解释性子字段、`outgoing_pressure` 的理由子字段。`episode-design.md` 增加
  3.8 节写明这条判据，`STY-12` 改为指向故事引擎，不再逐集重述。既有项目的多余字段不会被
  校验器拒绝，但新建项目不再要求填写。Golden Sample 与开发技能的模板已同步为同一形状。
- **单步因果链不再出现在分集记录里**。一集只有一步因果时，"目标 → 反对 → 结果 →
  出去的压力"已经把它说完了。单集卡的实现块相应不再写指向某一步的指针，直接写三个
  可见载体；样例的 8 张单集卡已按此更新。
- **产物引用不再携带哈希**。引用形状从 `owner + artifact + hash` 收敛为 `owner + artifact`，
  `sources` 条目同理。这些哈希在创作期是只写的：三个校验器只校验它的**格式**，没有任何一个
  重算或比对过字节。旧项目里多余的 `hash` 字段会被当作不支持的字段报出来，而不是静默接受。
  本版本随后把产物里剩下的哈希也一并删掉（见「移除」一节），所以真正还在比对字节的只有
  生命周期状态与交付包 `checksums.sha256` 两处，都由工具自己维护。
- **Python 下限降到 3.9**。审计发现整套脚本里真正需要 3.10 的只有一处
  `zip(strict=True)`，其余 89 处 PEP-604 注解都因为 `from __future__ import annotations`
  在 3.9 上本来就能跑。改掉那一处、给五个测试模块补上 lazy annotations、把一处
  `Path.write_text(newline=)`（3.10 API）换成 `write_bytes` 之后，**全套测试在
  macOS 自带的 3.9 上原生通过**——此前本机只能跑 48 项，其余被版本守卫直接挡掉。
  CI 矩阵同步改为 `3.9`、`3.10` 与 `3.14`，让下限真的被测到。五个脚本的版本守卫提示
  一并从「requires Python 3.10 or newer」改为 3.9，此前它报的下限和它实际检查的不是同一个。
- **`--input` 的完整性写进文档**。失效图就是 `publish --input` 这份清单：产物首行 `sources`
  声明了什么就逐个列什么，少列一个，那个上游改了它不会变 `update_needed`，会带着过期的绑定
  停在 `accepted`，而这与真的还是最新的看起来完全一样。身份与变体成对的上游尤其容易漏一半。
  图片提示词阶段补上了自己的完整命令示例。

### 移除

- **删除产物里的全部哈希**。清点后逐个查清了它们摘要的是什么，然后全部删掉：
  `derivation.input_hashes` 是上游产物的整文件摘要（`IMG-EP001-CHAR-01` 带的正是上一代
  `characters.jsonl` 与 `looks.jsonl` 的值），`derivation.rendered_hash` 是同一条记录里
  `generic_prompt` 那个字段本身的摘要，`recipe.hash` 连模板都没声明过，
  `end_report.source_end_hash`、`content_sha256`、`source_sha256`、`index_sha256`、
  `map_sha256`、`record_hashes`、`target_hashes`，以及观察记录模板里的
  `observed_media_sha256` 与 `valid_only_for` 三个哈希——全部移除。
  共同点是同一个：人手工填、没有任何脚本回头核对，于是每次上游编辑都要重算整条下游链，
  而漏算不会报错。清点时样例里 331 个此类值全部与字节对不上。

  **代价逐项实跑确认过，不要按字段名想象**：

  | 原来靠哈希的能力 | 现在 |
  |---|---|
  | 剧本索引按内容保 block ID | **没了**。索引不存内容，原地重跑按位置重编号；`--previous-index` 与 `--previous-source` 恢复成必须成对给出，靠重读改稿前的字节做内容比对 |
  | 原著漂移检测 | **退化**。`novel_index verify` 仍然报行数与编号不连续，所以插行删行照样发现；同行数的原地改写不再发现 |
  | 配音本相对剧本的漂移 | **仍在**。`VOICE_LINE_TEXT_DIVERGED` 逐字比对照样报；失去的只是 `VOICE_INDEX_IS_STALE_AGAINST_SCREENPLAY` 这个把「索引过期」与「表被改过」分开的诊断码 |
  | 多集整稿导入的 span 校验 | **基本仍在**。`source_byte_length`、byte span 有效性与顺序、行↔字节一致性都还在；失去的是逐行内容摘要 |
  | 创作者验收钉住字节 | **没了**。验收记录只记接受了哪些产物 |

  `CLAUDE.md` 增加一条规则：产物里不写任何哈希。三条断言把这些键列为已退役——样例的
  JSON/JSONL、技能的 JSON/JSONL 与 `assets/*.md`，外加一条按字面扫描 `skills/**/*.md`
  的（`references/*.md` 里的 JSON 块只有这一条拦得住，而那正是当初 `derivation` 溜回来的
  入口）。三条都反向验过：加回任一键会报红。

  **工具自己维护的那一层保留**：生命周期状态（`update_needed` 靠它）、交付包
  `checksums.sha256`（`verify` 靠它）、生产任务指纹。它们不写进产物，也不靠人维护。
- **删除 `short-drama/references/routing-examples.md`**。全仓扫描显示它没有被任何技能引用，
  其意图路由表已被 `SKILL.md` 的「意图路由」覆盖，且是中文技能里唯一的英文文件。文件里
  两条其他地方没有的规则已并入 `SKILL.md` 正文（产物可以直接入场、请求含糊时先展示状态
  并给出不超过三个动作），因此没有丢失行为规范。
- **接上 `look-development.md` 的入口**。README、技能描述、意图路由与 know-how 索引都提到
  Look Development，但没有任何文件告诉智能体去读这份资料，它实际不可达。现由「意图路由」
  一节按需指向。

### 说明

- **验收不再因为编辑而卡死**。实测 `publish → accept → 编辑产物 → accept` 全程可走通：
  编辑后状态变为 `update_needed`，再执行一次 `accept` 即回到 `accepted`，不需要重新
  publish。生命周期状态里的字节记录由工具维护、用于告诉创作者"你接受的不是现在这份"，
  它本来就不是死锁；真正卡死的是产物数据里手工维护的验收字节绑定，那部分已随哈希链移除。
- **音乐规格与声景的归属写清楚了**。环境声与事件音随镜头走，写在各自运动规格的 `audio` 里，
  已被镜头认领的 `[SFX]` 剧本块声明的声音要出现在这一镜；音乐承担跨镜、跨场或主题曲职责时
  才写独立的 `music-specs.jsonl`。此前 `music_spec_check.py` 没有项目路径，也没有链接入口。

## [0.4.1] - 2026-08-17

v0.4.1 把 v0.4.0 之后的工业化校准收进一个补丁版本。`set-authority` 补上长期缺的一环：
资产、图片提示词、分镜与视频提示词都要求读取已接受的制作形态与视觉方向，却没有命令能把
创作者的选择写回 `short-drama.json`。关键帧与运动交付正文各增加一遍写完之后的自查，
把「接手的人能不能照着画出来、照着拍出来」拆成逐项可对照的事实，每项都写清缺了会发生什么。
同一份上游快照被一个文件反复引用时改为在 `sources` 里声明一次，Golden Sample 的 `剧集/`
目录降到原来的 76.3%。两份示例合并到 `examples/`。

### 新增

- **`set-authority`：把创作者选择写回 `short-drama.json`**。`creator_authority/*` 与
  `format/target_seconds_per_episode` 只能由一条已发布且已接受的创作者决策记录写入，
  命令绑定该记录的 `decision_id`。此前资产、图片提示词、分镜与视频提示词四个技能都要求读取
  已接受的制作形态与视觉方向，但没有任何命令能把它写进去，`delivery_surface` 因此长期停在
  `unset`，分镜的目标时长差值分支也取不到值。
- **锁定镜头有了和运镜并列的写法**。lock-off 要写死构图（景别、机位高度、轴线侧）、
  这段时间里画面内还有什么在动、以及保持多久；摄影机一栏空着会被执行端读成可以自由漂移。
- **运动正文默认写成有序节拍链**。`首先/接着/随后/与此同时/最终` 各承担一段，动作的幅度、
  角度与锚点秒数属于本环节自己写的这一拍，正常写出来即可；只有在改写已接受时长或声称未被
  接受的空间尺寸时才越权。

### 变更

- **运动交付正文补齐可执行事实**。`motion-recipe.md` 增加一遍可执行检查：起手状态、串行链
  每步的方向与幅度、至少一处量级、一条并行通道、结束状态、摄影机（锁定写死构图与保持多久，
  移动写触发/路径/终点景别）、以及台词逐字挂在它发生的那一拍上——每项都配上缺了会发生什么。
  同时给出长度要求：这段是给生成器的执行指令，两百字上下即可写满，形容词与情绪解释不改变
  生成结果，只增加成本。
- **关键帧交付正文补齐可渲染事实**。`keyframe-craft.md` 增加一遍可渲染检查：人物位置与间距、
  前景/中景/背景、视觉重点、构图与景别、机位轴线侧与俯仰与焦段、光位、每个在场者的姿态朝向
  手部持物、以及哪些与上一镜相同——每一项都配上缺了会在画面上发生什么。同时给出密度要求：
  一件事只说一次，十二项写满约五百字，删掉任意一句若画面没有更不确定，那句就是多的。
- **`SHT-05`：边界条目不能整条写成回指**（`structural_invariant`，可能阻断既有产物）。
  `start_boundary`/`end_boundary` 的每一条只写不读上一镜也能理解的绝对事实；
  写成「同上」「位置不变」这类需要往回翻才能解释的条目会被 `storyboard_check.py` 报错。
- **资产接受记录必须指向创作者决策**（`structural_invariant`，可能阻断既有产物）。
  `creator_acceptance.decision_ref` 的 `record_id` 需以 `CD-` 开头，指向 `创作者决策/` 下的
  一条决策记录；指向剧本块或资产记录会被 `asset_check.py` 报错。
- **图片提示词规格如实写生命周期状态**。`status` 取 `candidate` 或 `accepted`，跟随该对象
  当前的接受状态，不再要求已被接受的权威文件继续自称 `candidate`。
- **景别按工作景别组织，不按单调收紧**。一场先开宽一档，第二镜落到工作景别，之后多数切镜
  在这一档上下一格来回，宽景与特写是离开工作景别的标记事件；景别阶梯补入 `中近景`。
  子镜密度给出可看带宽（大致每 4–8 秒一次），并说明镜头超过约五到八秒后，一张冻结关键帧
  只描述了开头，其余部分需要拆镜或由创作者明确接受为宽规格镜头。
- **规范化能吃下四类真实来稿**。画外通道按「角色名后紧跟 OS/VO、括号可有可无」识别，
  先剥掉后缀再比对本场人物，避免把 `角色OS` 整个当成新角色；没有任何符号、动作直接成段的
  来稿同样有结构，靠人物行与台词行反衬即可；场次编号、时段、内外三槽都可能整槽缺失，
  缺哪一槽空着哪一槽——补出来的时段会直接变成整场的布光依据。
- **角色识别锚点覆盖侧面与背面**。锚点集合至少各有一条在侧面与背面成立的可观察特征，
  并写出全组可比较的尺度次序与一个绝对参照点。
- **剧本落定后做一次集长量级粗测**。正文字数按已接受的语速比折成秒数与目标时长比一次，
  差出两倍以上就在报告里点名。
- **上游引用改为紧凑写法（修复 #39）**。同一份上游快照被一个文件反复引用时，在 `sources` 里
  声明一次：`.json` 用顶层 `"sources"` 字段，`.jsonl` 用首行 `{"record_type": "sources", ...}`
  记录，`screenplay-index.jsonl` 折进已有的 `screenplay_index_meta` 首记录。单条引用写成
  `{"src": "shots", "record_id": "SHOT-EP001-01"}`，按需带 `field`、`authority`、
  `value_seconds`；`authority` 仍属于单条引用，同一快照可以同时被 `accepted` 和 `candidate`
  引用。只被引用一两次的快照直接写在引用里——声明本身也占篇幅。
  各 Skill 的校验器同时接受这两种写法，已有项目不需要改写；`src` 指向未声明的键会报
  `REF_SRC_IS_NOT_DECLARED`。
  Golden Sample 的 `剧集/` 目录由 926,123 字节降到 706,286 字节（-23.7%）；单集分镜里
  需要逐字誊写的 64 位 sha256，`shots.jsonl` 从 69 个降到 8 个，`keyframes.jsonl`
  从 119 个降到 27 个。
- **新增 `tools/compact_refs.py`**。按依赖顺序改写一个项目的引用写法，只在文件确实变短时改写；
  `--check` 核对每条引用都能解析、且绑定的 hash 等于被引用文件的当前字节。
- **两份示例合并到 `examples/`**。原 `demo/` 迁到 `examples/excerpt-chain/`，与
  `examples/golden-project/` 并列，新增 `examples/README.md` 说明各自回答什么问题：
  摘录链条看一个环节交给下一个环节什么，Golden Sample 看完整项目的落盘形态与引用链。
  README 同时写清摘录链条的范围——剧本、资产设定与图片提示词、分镜、视频提示词，
  不含关键帧与审查结论。

## [0.4.0] - 2026-08-16

v0.4.0 把十个 Skill 拆成可独立安装、可离线自证的单元：删除套件级 hash 清单、sibling pin
与完整安装门禁，每个 Skill 自带最小样例、结构校验器和 `selftest.py`。新增
`$short-drama-produce`：预览准确 job、取得创作者明确确认后，才通过项目外 adapter 执行图片、
视频、TTS 与时间线音乐生产，并可选支持 Seedance、GPT Image 2 与 MiniMax Music。
项目生命周期从恢复日志、快照与传播闭包收敛为每项产物一个创作者可读状态；全链预览与“继续”
改为每轮一个有界工作单元，做完就把控制权交还创作者。同时新增可校验的八集 Golden Sample
《善意不结账》，并把创作台重排为项目概览、分集进度与已有媒体的轻量展示层。

### 变更

- **移除 Suite 级耦合门禁**。删除全局文件 Hash 清单、所有 `suite-ref.json`、完整十技能安装
  校验和 Suite capability 声明；每个 Skill 可独立安装，缺少 sibling 不再阻断当前任务。
  `project_tool.py` 同时移除业务路径到技能名的中央注册表，只保留合法阶段目录、受保护目录、
  原子发布和项目内单一 artifact 路径所有权。Dashboard 继续只读取项目状态。
- **项目生命周期大幅瘦身**。`project_tool.py` 从恢复日志、快照、传递过期闭包、五条并行
  状态轴和审查 provenance 证明，收敛为每项产物一个创作者可读状态。发布仍验证全部来源并
  在项目锁下逐文件原子替换；接受和复核只绑定当前输出；`status` 在读取时检查输出与直接输入，
  不把变化传播写回全项目。交付包继续使用 manifest 与 checksums 做准确性复核。
- **命令与指令同步简化**。移除 `recover`、`--input-record`、用户提供 target/evidence hash
  等操作；审查隔离从无法由文件证明的强制身份协议改为明确的质量建议，CLI 只记录 verdict、
  reviewer 标签和备注。v0.3 状态可继续读取，并在下一次状态变更时迁移到紧凑格式。
- **Dashboard 明确保持轻量**。它只负责内容展示、已有媒体预览与有限文本编辑；创作、复核、
  生产和交付能力仍由 skills 与项目命令承担。
- **创作台展示层重排**。增加项目概览、分集进度条带与已有媒体画廊，并支持项目内 TTS/音频
  预览；不增加生产 API、供应商配置、任务提交按钮、数据库或画布编排。
- **指令改为正向表述**。各阶段 `SKILL.md` 的批次说明统一为中文的「每轮的工作单元」，按
  “只读直接输入 → 落盘本单元 → 报告已覆盖/剩余范围 → 交还控制权”四步写清一轮要做什么。
  阶段契约、运行时预检、README 与贡献指南改为直接描述当前形态，不再逐条罗列已经移除的
  机制（恢复日志、传播图、Suite 清单、sibling pin）。

### 修复

- **全链预览改为有界续跑（部分修复 #34）**。全链预览和“继续”每轮只执行一个 owner 的一个
  明确批次；写作、资产、图片提示词、分镜和视频提示词都在当前场景/记录范围完成校验、落盘并
  报告剩余范围后交还控制权，下一阶段、复核与生产各自需要明确的后续请求。复核也只执行一次
  有界 pass；无法取得隔离上下文时诚实自检，不再等待或重试 reviewer provenance。
  同时移除遗留的全局 `current_checkpoint` 与资产 C2/五轴放行表述，避免旧生命周期重新成为门禁。

  这解决的是“跑起来就停不下来”：每轮都会回到创作者手里，可以随时叫停或改方向。
  单集总产出量本身还没有变小——一集要写的结构化 JSON 仍远多于剧本正文，见 #39。

### 新增

- **十个 Skill 都可独立离线自证**。补齐 core、开发、原著分析、写作、分镜、视频提示词和生产
  Skill 的 `Quick Start` 与负例 selftest；CI 会把每个 Skill 单独复制到陌生路径、从无关 cwd
  运行，证明它不读取 sibling、全局 manifest 或 Dashboard。视频提示词 Skill 同时新增独立的
  时间线音乐规格与结构校验，不把配乐复制成每镜供应商字段。
- **确认后生产新增三家可选 adapter**。`short-drama-produce` 在不改变上游中立规格的前提下，
  支持 GPT Image 2 图片生成/参考图编辑、Seedance 显式模型的 text-to-video，以及 MiniMax
  Music 3.0 主题曲/纯配乐。凭据和 adapter config 仍在项目外；官方未证明的字段、透明背景、
  本地 Seedance reference 上传和错误媒体签名都会 fail closed，所有远端执行仍消费一次明确确认。
- **三个阶段 Skill 补齐独立可运行闭环**。`short-drama-assets`、
  `short-drama-image-prompts` 与 `short-drama-review` 各自新增原生最小样例、本地结构校验器、
  离线 selftest 和 `SKILL.md` Quick Start；单独拷走目录后仍可运行，不读取 core、sibling、
  全局 manifest 或 Dashboard。校验只覆盖可机械证明的 ID、引用、绑定与 verdict 一致性，
  不把审美判断或固定配方升级成硬门。
- **`$short-drama-produce` 确认后投产**。新增 provider-neutral 的图片、视频与 TTS job：
  `prepare` 展示准确数量、prompt/spec、参考、参数、输出、覆盖行为与 adapter；只有创作者看到
  当前预览并明确确认后，`run` 才以无 shell 的 argv adapter 执行。job 或直接输入变化会让确认
  失效；adapter 一旦启动即消费确认，失败重试也要再次确认，避免隐性重复费用。
- **项目外 adapter 与项目内结果记录**。凭据留在环境或系统凭据存储，adapter 配置只在项目外
  保存命令与超时。结果按确认 targets 原子落到 `production/制作成果`，项目只保存精简运行状态、
  媒体类型、大小和交付校验和。离线 fixture 覆盖 image/video/TTS 三条真实执行路径。

## [0.3.0] - 2026-08-13

v0.3.0 新增长篇原著分析、多集整稿断点接入与本地短剧创作台，补齐输出语言、
Look Development、生产观察校准和更严格的项目生命周期，并修复 Windows 安装校验。
正式版在发布候选基础上补齐原著索引失败路径、分析产物所有权、结构化引用完整性、
VID-04 非法数值、跨平台路径唯一性、旧事务安全阻断及 Windows 交付验证。

### 新增

- **多集完整剧本按 Agent 判断切片并可断点续跑（修复 #27）**。`$short-drama-develop`
  新增 `episode_intake.py`：自动标题只产生候选索引；遇到非标准结构时，Agent 根据文件实际
  特点提供行边界，工具再绑定原始字节 span/hash。后续每次只读取当前集，批次由 Agent 结合
  实际 `byte_length`、语义复杂度和可用上下文明确选择，不内置固定集数。分集地图内容仍由
  Agent 判断与撰写；脚本只负责 source drift 校验、原子合并、洞位恢复和幂等重放，避免把
  54 集整稿或整季 map 塞进内联 Python 后因上下文/命令截断中断。Windows CI 同时覆盖带中文、
  空格与 CRLF 的索引/切片 CLI。
- **角色声音方向落到 `$short-drama-assets`，以参考音频为载体**。项目需要跨集保持声音时，
  Character 记录的 `voice_direction` 现在以**创作者授权的参考音频绑定**为主字段：
  短剧的音色由一段参考音频承担（试音、声音库、授权克隆源），不由一段文字描述承担。
  没有参考时保持 `reference: null` 并标「待选型」，不用形容词冒充音色身份。
  绑定沿用套件既有的参考纪律（`may_control` / `must_not_control` /
  `admission_status`）：那条参考里的**情绪、录音空间、背景内容**永远不进身份，
  「绑定不等于听过」，没有创作者说明或授权试听记录就保持 `unverified`。
  文字负责参考音频承担不了的两件事：选型与验收判据（每条带反例）、以及专名发音
  （只能有一种拼法，`AST-10` 结构性阻断）。新增 `AST-08`–`AST-12`、
  `references/voice-direction.md` 与派生的 `设定集/voice-casting.md`（缓存，非权威）。
  参考音频本身留在 `输入/`，不进 设定集，也不进交付包。
  **仍不生成音频，也不调用任何语音、合成或克隆服务**。
- **`$short-drama-novel-analyze`：长篇原著分析技能**。把小说、连载或多集散稿拆成可追溯的
  分析层：章节索引（脚本）、改编价值快评、逐章功能提取、剧情单元与节奏情绪聚合、
  人物与设定候选、改编价值判定与分集候选。产出全部落在 `项目开发/source-analysis/`，
  归本技能所有。**它只产候选，不产契约**——`项目开发/adaptation-map.jsonl`、创作简报与
  分集地图仍归 `$short-drama-develop`，分析可以被推翻，已接受的改编承诺不能。
- **管道在改编价值快评后停靠一次**。快评用 `novel_index.py sample` 取一批等距、
  首尾必取、可复现的章，判断故事框架、三类判定比例、开篇替换点、制作负担量级、
  三处风险与分集量级，然后问创作者要不要全量拆。判据是全书的改编密度，
  所以抽样横跨整本书而不是只读开头——开头慢而爽点密的书能改，开头漂亮而后段全是内心戏的
  书不能改，只读开头这两类都会看反。快评第一行写覆盖率，结论限于抽样范围；
  S5 完成后回填对照，写清哪几条判断被推翻。
- **`scripts/novel_index.py`：唯一切片真源**。章节边界只算一次并绑定 `content_sha256`，
  供后续每个阶段共用；识别中文数字章号（含 千 / 两，覆盖千章以上连载），
  只认一种编号单位（章/回/节里出现最多的那个），剔除开头目录块，按卷分段校验编号，
  并提供 `verify`、`sample`、`coverage` 三个确定性检查。
- **`$short-drama-review` 新增 `source_analysis` 范围与审查表**。原著分析层此前没有
  可用的审查范围，技能却把质量结论路由给 review；现在 `rubric-source-analysis.md`
  覆盖结构校验、抽样声明、事实可溯源、功能提取、归并质量与候选集判据。
- **输出语言契约落地，并接到真正的消费方**。`short-drama.json#/language` 此前只在 `init`
  写入、之后没有任何地方读取；现在它是创作者可读产物的权威，并新增
  `#/format/prompt_language`（默认 `en`）管送给图片/视频生成器的提示词正文。两个字段在
  `init` 校验格式（格式错误当场拒绝，因为下游没有任何地方会再检查一次），由 `status`
  一并报出，并由 `project_languages()` 统一解析旧项目的缺省值。
  `$short-drama-image-prompts`、`$short-drama-video-prompts`、`$short-drama-storyboard`
  与 `$short-drama-review` 改为读这两个字段，不再各自硬编码「中文项目使用中文」——
  此前 storyboard 还把可复制提示词和创作者可读文本并成同一条语言规则。
  **记为新增**：默认行为与此前一致（中文项目仍出中文、提示词仍可沿用既有写法），
  创作者可覆盖任一字段。

- **能力说明的第三种状态**：`production-prompt-grammar` 的「按已知能力调整写法」此前只有
  支持/不支持两态，漏掉了中间那一段——仍在允许范围内、但需要反复重试才拿到可用结果的
  不稳定区。它最容易被漏掉，因为它不报错：允许范围的上界回答「能不能提交」，稳定区的上界
  才回答「提交一次大概率可用」，差额由重试补齐而代价由项目承担。现在它被写成**写作阶段的
  决策点**（减少同镜并发主体、拆镜、把复合动作分到两镜），而不是提交后再观察的现象。判断
  不依赖任何具体数字，只看创作者说明里是否出现「效果较好」「稳定性下降」这类措辞；说明只
  给了允许范围时不自行推断一个更保守的数字当成事实。

- **内容效果增强层**：Look Development 新增人物表现、核心地点、高压力场景三类可选风格帧；
  storyboard 新增可选的场次视觉戏剧计划与 Coverage Audition；video-prompts 使用逐角色
  `performance_arcs[]` 与 `attention_handoffs[]`，并投影场次声音策略。所有新增层都由创作者
  接受后再向下游投影，不拥有剧本、资产或 shot 边界，也不规定宫格、方案数或镜头数。
- **项目级生产反馈校准**：新增区分 `input_reference` / `generated_result` 的生产观察模板和
  review 方法；结果观察绑定准确 prompt/spec、稳定参考槽位与制作配置，只在当前项目版本有效。
  修订一次只改变诊断所需变量并记录 `preserve_set`，不从任务状态推断质量。
- **维护者提示词学习轴**：`short-drama-knowhow` 增加题材承诺 × 制作/视觉语言 × 场景功能 ×
  提示词职责 × 版本角色的定性阅读方法；继续禁止词频、匹配器、任务成功率或供应商字段生成创作规则。
- **次级文本契约的配套模板**：`slot_id` / `order`、修订 lineage 与 readiness 三项规则本身按
  约束力记在上方**变更**；这里只记它们的可复制载体——参考绑定模板补上两个字段，拆并镜
  lineage 与准备度分别有独立 fragment 和派生写法，创作者不必自行发明结构。
- **可选层减负**：Look Development 使用独立规格模板，不污染普通资产图片提示词；普通 shot 省略
  空的场次计划/修订谱系，普通 motion 省略重复目的、场次计划、母版范围与 readiness 字段；Coverage
  Audition 默认只留项目创作过程，打包时显式 `--omit`。表演/注意交接、补拍范围与拆并镜 lineage
  都迁入按需 JSON fragment，既不污染普通记录，也不让 agent 临时发明字段。
- **本地短剧创作台**：新增零生产依赖的 Python 本地服务和单页工作台，以内容目录和当前
  正文浏览 `short-drama.json` 工程；支持 UTF-8 文本编辑、
  SHA-256 乐观并发、
  原子保存、Markdown/JSON/JSONL 安全预览、结构化数据双端校验、创作者可读生命周期卡片，
  以及带预演/待审标签的图片和视频只读预览与视频 Range 请求。服务只绑定回环
  地址，并拒绝异常 Host/Origin、符号链接、路径穿越和对项目清单、运行状态及交付包的写入。
- **中文项目目录**：新项目使用 `输入/`、`项目开发/`、`设定集/`、`剧集/`、`交付/`、
  `创作者决策/` 与 `审查/`。现有英文目录继续支持；第一次阶段发布把项目级布局
  固定进运行状态，后续发布、打包与校验沿用同一布局。检测到中英文平行阶段树时，状态会
  报告 `mixed` 并暂停新发布，已存在且集号唯一的交付包仍可校验。
- **字幕与演示展示**：项目控制台支持编辑 SRT/ASS 字幕源文件；README 增加中文项目控制台
  截图，并嵌入 15 秒原生音画中文字幕成片。
- **`verify` 子命令**：用交付包自带的 `checksums.sha256` 复核它。此前该文件写出后
  没有任何命令再读，交付目录被事后改动仍然"看起来已交付"。除逐个复核 hash 外，还报告
  **未登记的新增文件**——校验和清单对新增是盲的。
- **`motion_timing_check.py`**（video-prompts）：`VID-04` 此前是被声明为
  `structural_invariant`、诊断码 `VID_EXPLICIT_TIMING_OVERFLOW` 也已分配、却没有任何
  脚本读取 `motion-specs.jsonl` 的一条规则。新脚本做显式分段的算术核对。超出按**终点**
  判断、不足按**裁剪到镜头长度内的并集**判断，因为中间留空加尾部超出会互相抵消：
  `0.0-2.0` 加 `3.0-5.0` 在 4 秒镜头上"合计正好 4 秒"，实际却同时发生了截断与留白。
  `relative` 计时不做算术断言，列在 `relative_plans` 中报告而不是判过。

### 变更

- **Windows 安装不再把 CRLF 换行误判为套件混装（修复 #26）**。清单生成器与运行时校验器现在都按
  规范化后的 LF 文本计算 SHA-256；仅 CRLF/LF 不同的文件保持等价，真实内容变化仍会阻断。
  CI 新增真实 `windows-latest` 安装回归，覆盖此前 `/short-drama` 初始化在预检阶段被
  `short-drama-assets/SKILL.md` hash mismatch 阻断的问题。机器可读 CLI JSON 也改为 ASCII-safe
  转义，Windows 仍使用旧控制台代码页时，中文项目路径与标题不再导致输出阶段编码失败。

- **短剧创作台的界面细节整轮返修**。内容目录里每行的汉字圆牌换成一套笔画一致的线描图标；
  集号与场次号改用等高数字，此前正文衬线把 `EP001` 渲染成 `EP0o1`；搜索框、项目下拉与
  按钮的描边补到 3:1 以上，深色底上此前几乎看不出它们的边界；当前打开的那一份内容改用
  主色描边标出。正文载入时先用骨架占位撑住版面，载完再换成正文。界面字号改用 rem，
  浏览器缩放与系统字号设置现在生效，中文小字下限提到 12px。「已保存」是状态不是动作，
  不再显示成一块暗掉的主色按钮。界面字体不再点名 Inter——它没有随页面下载，
  装没装 Inter 的机器此前渲染结果并不一致，现在统一走系统界面字体加系统中文字体。

- **新增一个技能目录，安装时必须与其余技能保持同级**。套件清单从 8 个公共技能变为 9 个；
  只链接旧的 8 个目录会让 `suite_verify.py` 报缺件并停止写入。升级时重新链接整个
  `skills/` 目录，不要混装版本。

- **项目控制台收敛为单页「短剧创作台」**。此前的多区块控制台把文件树、真实路径、
  结构化格式、生命周期键和工程详情直接摆在创作界面上；现在左侧只有按项目与剧集整理的
  内容目录，右侧只有一份正文或画面。**记为变更**：`创作者决策/`、`审查/` 与 `交付/`
  不再出现在控制台里，习惯从这里翻自己的接受决定或审查 finding 的创作者，需要改用
  `$short-drama-review` 与 `$short-drama` 的生命周期命令，或直接打开文件；控制台不再提供
  入口，也没有开关可以调回来。字幕、`.json` 与 `.jsonl` 仍可在控制台里编辑，保存前
  由服务端校验格式。放在标准目录之外的创作文件会归入「其他内容」，不会从目录里消失。

- **发布时拒绝未填的结构化引用 `hash`**。此前 `hash` 不是 64 位十六进制的引用会被静默
  跳过，不计入依赖边，于是「精确输入」交叉校验根本不会对它运行——照抄模板、原样发布的
  候选反而最容易通过，**填得越少、检查越宽松**，正好与 hash 绑定的目的相反。现在这类
  引用报 `structured ref hash is unfilled or invalid`。同一文件里给生命周期证据引用做
  校验的 `_normalize_artifact_ref` 早已是这个行为，本次只是把发布路径对齐。
  `*_locator` 对象不带 `hash` 键，不受影响。
- **ArtifactRef-like 结构化引用必须完整声明 `owner`、`artifact` 与 `hash`**。已出现其中
  任一依赖字段的 `*_ref` / `*_refs` 若缺其他字段，不再被静默当成普通对象跳过，避免下游
  发布成功却没有依赖边。本地 `record_id` / `field` 引用及只负责定位的 `*_locator` /
  `*_locators` 保持原行为；普通对象里的 `metadata.hash` 也不会被误判成跨产物依赖。
- **项目路径在所有支持平台上保持唯一拼写**。发布、引用、打包与校验共用的路径入口现在
  拒绝控制字符、Windows 保留设备名、非法字符以及以点或空格结尾的分量，避免同一路径在
  Windows 上归一到另一负责技能的规范产物并绕过所有权检查；同一请求、已跟踪状态、结构化
  引用及交付选择也会拒绝仅大小写不同、或 Unicode NFC/NFD 规范等价的路径别名，旧事务若含
  此类别名会安全阻断而不重放。
  该回归已加入 Windows CI。
- **分集目录每个集号只有一种拼写**：`EP` 加三位数字，超过 `EP999` 之后不再补零
  （`EP001`、`EP1000`）。`episodes/ep1/`、`episodes/EP1/`、`episodes/EP0001/` 都会被
  拒绝。此前它们可以发布成功，随后被交付完整性闸门的 `episodes/<EP>/` 前缀枚举静默
  跳过，于是闸门会在一个它从未清点过的分集上通过——正是该闸门自己声明要防止的失效。
  结构化引用与 `package --include` 里的分集路径同样适用。
- **`short-drama.json` 与 `delivery/**` 不再是合法发布目标**。前者承载
  `creator_authority`，此前任何技能都能整份覆盖掉已确认的视觉方向与制作配置；后者只应
  由 `package` 闸门写入，此前已交付的 `manifest.json` 可以被事后替换，而校验和文件不会
  被重新读取。`package` 自身通过内部参数opt-in，不依赖创作者可控的 `stage` 名。

- **发布只接受标准阶段目录，已声明产物的负责技能固定**。此前任意项目内相对路径与任意
  `owner` 都能发布：写错一个字母的 `epsiodes/EP001/` 会建出一棵平行目录树，而 `status`
  从不报告它；任何技能都能发布任何别的技能的产物。现在根目录限于 `项目开发`、
  `设定集`、`剧集`、`创作者决策`、`审查`，确需别处的临时文件加
  `--allow-unregistered-path`——仍然可行，但不再是静默的。负责人约束只覆盖各
  `SKILL.md` 已声明的产物名（含 `设定集/` 的六份身份账本，负责人均为 `short-drama-assets`）：
  一个分集目录里住着四个技能的产物，按目录前缀反推负责人等于替契约回答它从未回答的
  问题，会在没有授权的情况下拒掉创作者合法放在旁边的临时文件。
- **所有路径判断改为忽略大小写**。本套件在 macOS/Windows 的大小写不敏感文件系统上开发
  和运行，`Delivery/EP001/manifest.json` 与 `delivery/EP001/manifest.json` 是同一个
  文件；区分大小写的判断在那里等于没有判断。同一次修正也补上了此前就存在的 `Inputs/`
  与 `.Short-Drama/` 绕过。
- **`short-drama.json` 按文件名拦截，不只是根目录那一份**。`find_project` 向上查找，
  被放进子目录的同名文件会让该子目录冒充项目根，创作者在其中运行 `status` 会读到
  伪造的项目。
- **`VID-04` 新增六个 `structural_invariant` 诊断码**（`VID_EXPLICIT_TIMING_SHORTFALL`、
  `VID_EXPLICIT_TIMING_UNDECLARED_OVERLAP`、`VID_EXPLICIT_TIMING_UNPARSEABLE`、
  `VID_DECLARED_TOTAL_MISMATCH`、`VID_DURATION_PROJECTION_STALE`）与
  `VID_TIMING_MODE_INCONSISTENT`。此前只有超出方向有编码，不足方向的后果其实更重：
  未分配的余量不会渲染成静止画面，执行端会用没有上游来源的动作把它填满。**记为变更**：
  既有运动规格若把重叠关系写在 `phases` 散文里而没有置 `timing_plan.declares_overlap`，
  现在会被判为未声明重叠而阻断，需要补上该字段。
- **多人表演字段由单数迁移为可选逐角色数组**：既有 `performance_arc` 若确有可见表演变化，
  应改为 `performance_arcs[]` 并绑定承担变化的 `actor_ref`；只有注意真正交接时才补
  `attention_handoffs[]`。普通单人、空镜、道具或纯空间运动省略这两个可选字段；启用时使用
  独立 fragment，不让 agent 自行发明结构。
- **Coverage Audition 的接受记录同时承载方案选择**：audition 本体不再内嵌事后
  `creator_selection_ref`；独立 `artifact_acceptance` 决定绑定其准确 target hash，并记录
  `selected_audition_record_id` / `selected_approach_id`，正式场次计划再同时引用两者。这样既能
  证明创作者选了哪一案，也不因把决定写回已接受文件而制造 hash 循环。
- **多参考绑定必须写稳定 `slot_id` 与显式唯一 `order`**（`IMG-12`、`VID-17`，均为
  `reviewed_invariant`）。此前参考的用途只由数组位置决定，插入或重排一条参考就会静默改变
  其余参考的角色，而 `hash` 不变、审查也看不出来。**记为变更**：0.2.0 及更早写出的
  `剧集/<EP>/assets/image-prompt-specs.jsonl` 与 `剧集/<EP>/storyboard/motion-specs.jsonl`
  里带两条及以上 `reference_bindings` 的记录都没有这两个字段，升级后再送审会被判为
  槽位未声明而阻断。迁移方式见下方“升级既有项目”。
- **新增六条只约束本次新增可选层的 `reviewed_invariant`**：`IMG-11`（lookdev 风格帧的绑定与
  越权边界）、`SHT-19`（coverage audition 的方案必须真正不同，并由独立接受记录指名所选方案）、
  `SHT-20`（镜头重排、插入、拆分、合并的身份与 retire 规则）、`VID-16`（多人表演只让真正
  承担变化的人进入）、`VID-18`（逐镜文本准备度是按 scope 派生的投影，不是持久化的 motion 事实）、
  `REV-10`（项目校准 finding 的绑定与有效范围）。按约束力它们记为变更；但六条各自只在本次
  新增的可选层被启用、或下一次镜头修订发生时才生效，**已接受的既有产物不需要迁移**。

- **`reference-media-and-pickups.md` 拆成三份**。原文件在 146 行里塞了 **41 个不同的字段名**，
  同时讲三件互不依赖的事——参考图能决定什么、观众此刻能知道什么、补拍与母版的关系——
  它开头那句"三个容易被混在一起的问题"正是读者的实际体验。现在拆为
  [`reference-roles.md`](skills/short-drama/references/reference-roles.md)、
  [`audience-reveal.md`](skills/short-drama/references/audience-reveal.md)、
  [`pickup-and-alternate.md`](skills/short-drama/references/pickup-and-alternate.md)，
  各自独立、各自带一个具体例子（构图绑定、声音放而脸扣住、缩小范围后丢掉的那条要求去哪了）。
  **记为变更**：规则与契约一条未改，只有文件路径变了；引用旧路径的外部链接需要更新
  （`demo/` 下两处已同步）。首尾帧契约随"参考图能决定什么"迁入 `reference-roles.md`；
  `observation_kind` / `evidence_state` 段落与 `audience_visibility`、`motion_field`
  两个字段名在首版拆分中曾被漏掉，已回填——独立审查发现 `VID-20` 恰好依赖
  `generated_result`，而同一次提交删掉了它的定义。

- **视觉/运动审查表拆开复合问题**。此前单条 checklist 最长 492 字符，一句话里问四五件事，
  审查者无法逐条回答，实际结果是整条跳过。现在按"一条只问一件事"重写，最长 251 字符。
  判据内容一条未删。

- **交付路由与执行触发词成为显式契约**（`VID-19`、`VID-20`）。此前套件只描述了「打包」
  一个维度：容器决定一次交付装几个已接受镜头。但有些执行方式还决定**这一次从哪里开始生成**，
  而「从上一次生成结果续接」的起点是一段媒体，不是任何已接受产物——此前没有任何规则说明
  它该按什么记账。现在续接路由必须绑定 `generated_result` 观察记录或保持 `unverified`，
  已接受的镜头起始边界仍是唯一权威，不得把观察到的状态回写成边界；逐镜、多镜打包与单次
  长生成三条路由只改变打包粒度，镜头边界与逐镜可审查性不变。同时新增第二类「不可改写文本」：
  创作者档案声明的**执行触发词**必须逐字节出现且留在逐字对白围栏之外。它与台词的失败方式
  相反——台词被改写会念错，触发词被改写**什么都不会发生**，执行端静默退回默认路径，产物看
  起来完整而功能未生效。**记为变更**：只有声明了相应档案的项目才触发这两条，未声明路由或
  触发词的既有产物不需要迁移，套件也不自造任何字样。

- **结果处置判断先于修订**（`REV-11`）。此前 `project-calibration` 从诊断直接进入
  「单变量有界修订」，等于默认每个缺陷都该由提示词来修——而这通常是最贵的假设。现在校准
  finding 必须先在保留、后期处理、局部编辑、重新提交、改写五种处置里选定一种并说明依据：
  缺陷是否落在文字能控制的范围内，以及同一缺陷是否已经复现过。重复提交同一文本、以及在
  提示词上追加「高质量、避免变形、保持一致」一类形容词，都明确**不算修复**：前者为同一个
  错误反复付费且不产生新信息，后者不改变任何可执行事实却让下一次失败失去可比性。补拍与
  替代版的记账只在选择了局部编辑或改写之后才建立。**记为变更**：既有校准 finding 在下次
  修订时补写处置即可，已接受产物不受影响。

### 修复

- **多集接入索引不再绕过公开生命周期**。RC 文档让 `episode_intake.py` 直接覆盖 develop
  拥有的正式索引，与同一阶段“只通过 publish 写入”的契约冲突。现在自动或 Agent 手工边界
  先写 `_work/episode-intake-index.next.json`，验证后再经 WAL 发布到规范 owner 路径。
- **原著分析产物所有权与声明对齐**。`_progress.md` 和 `chapters/*.md` 已由技能契约声明归
  `$short-drama-novel-analyze`，但发布闸门此前只保护固定文件名，其他技能仍可覆盖这些
  分析产物。现在以受限文件族强制负责人，同时不占用嵌套目录或其他类型的创作者文件。
- **Lookdev 风格帧引用现在可从新建项目模板解析**：模板与参考资料一直要求
  `direction_ref.field` 绑定
  `/creator_authority/visual_direction/choices/look_development`，但新建项目的
  `choices` 是空对象，引用在创作者接受视觉方向前没有合法落点。模板现在预留该字段为
  `null`；接受决定仍负责写入实际选择，未接受状态不会被误判为已有视觉方向。
- **统一首尾帧契约**：核心参考不再声称套件只有 start artifact；现与 storyboard 一致为默认
  start、执行方式需要时可增加 end，且 end 只投影 `end_boundary`，不是第二个终点权威。

- **只有能证明布局的目录才能固定布局**：非标准目录与 `输入/` 不参与布局检测，却能通过一次
  `--allow-unregistered-path` 发布把整个项目钉死在另一套布局上。一个全中文目录的新工程
  向 `publicity/` 写一次，就会被记为 legacy——磁盘上并不存在任何英文阶段目录——此后所有
  中文发布都被拒绝，且没有任何受支持的方式撤销。
- **Dashboard 信息架构回到制作产物**：移除为项目宣传样片临时增加的“宣发素材”入口，
  媒体现在跟随项目开发、剧本、资产设定、分镜视频或审查交付中的所属产物显示；新项目也
  不再创建不属于技能产线的 `宣发/` 空目录，`status.layout.roots` 也不再声明
  `publicity`；非标准文件仍可从“全部”查看，不会被归入某个制作阶段。
- **Dashboard 并发首屏不再偶发找不到项目**：文件树与状态请求会同时重新发现项目，过去
  共用同一个目录游标时会互相推进扫描位置，其中一个请求可能返回 `project not found`。
  每次发现和项目内读取现在都使用独立目录游标，包括工作区本身就是项目的情况，
  并有并发回归覆盖。
- **布局冲突的已提交事务改为阻塞而不是卡死**：回滚前进时 `_apply_intended_lifecycle`
  抛出的冲突未被接住，STATE_APPLIED 永远写不出去，事务一直停在 `needs_rollforward`，
  每次 `recover` 都以同样方式失败，而 `blocked_transactions` 为空，创作者拿不到任何
  处理入口。现与其他冲突一致：记为 blocked 并给出冲突现场。
- **`verify` 重新区分「这一集从未打包」与「交付路径不安全」**：`FileNotFoundError`
  也是 `OSError`，两种情况此前塌缩成同一条敌意路径报错。
- **Windows 现在也可执行交付包 `verify`**。安全目录文件描述符只在 POSIX 存在，旧实现却把
  它用于公开生命周期命令，导致 Windows 在入口处失败。Windows 后端现在逐层拒绝链接与
  reparse point，并复用同一套清单认证、hash 与未登记文件检查；真实 Windows CI 覆盖
  package → verify。
- **旧事务里的大小写路径别名不再进入恢复重放**。manifest 校验同时检查 read set、target
  与 snapshot pointer 的可移植路径身份；旧版本留下的冲突拼写会持久标为
  `NONPORTABLE_LEGACY_PATH`，保持创作者文件不变并进入 `resolve_conflict`。
- **原著索引 CLI 与手写边界校验补齐失败路径**。`index --out` 会创建文档约定的
  `source-analysis/_work/` 父目录并原子替换索引，UTF-8 BOM 不再隐藏第一章，机器 JSON 在旧代码页下安全输出；
  `verify` 同时检查字段类型、连续 sequence、无重叠无缺口的行覆盖、chapter_count 与 hash，
  非法手写边界会返回可读问题而不是 traceback 或错误通过。
- **显式运动时序拒绝非有限值、布尔值与负数**。JSON 中的 `NaN` / `Infinity`、Python 布尔值
  及负时长此前可让 VID-04 算术检查错误通过；现在统一产生阻断 finding。
- **控制台的两处请求会话直接断开而不返回响应**：含非 ASCII 字节的会话凭证让
  `hmac.compare_digest` 抛 `TypeError`（认证前即可触发），顶层 JSON 不是对象的
  `short-drama.json` 让状态查询抛 `AttributeError`；两者都发生在处理器的 try 之外，
  浏览器只看到网络错误。现分别返回 401 与 500，且任何未预期异常都会被应答。
- **事务锁在并发首次创建时不再假失败**：macOS 上 `openat(O_CREAT|O_NOFOLLOW)` 输掉创建
  竞争会返回 ENOENT 而不是打开赢家刚建好的文件。锁文件的创建按定义无法被自身序列化，
  改为有界重试。
- **重新打开当前文件会静默丢弃未保存修改**：保存冲突后重新点击同一个文件是最自然的反应，
  而路径相等的短路让这条路径跳过了未保存提示。

- **剧本集标题与分集目录此前用两套不兼容的形式**：`project_tool.py` 接受
  `EP[0-9]{3,}`，`screenplay_index.py` 只接受 `EP\d{3}`，于是 `EP1000` 是合法目录却是
  非法集标题。两处统一为「三位数字直到 EP999，其后不再补零」，`EP0001` 被拒是因为它会
  成为 `EP001` 的第二种拼写，而完整性闸门按前缀枚举，另一种拼写下的产物会被静默跳过。
- 文档中 `episodes/EPxxx/`、`episodes/EPX/` 两种写法会被读成字面 ID，现统一为
  `episodes/<EP>/`。
- **集号与场次号收紧为 ASCII 数字**。此前用的 `\d` 是 Unicode 数字类，`# EP００１`
  能解析通过，于是全角与半角写法在 `BLK-` 身份命名空间里成为同一场景的两种拼写。
  既有全角稿件现在会报 `invalid_episode_heading`，改成半角数字即可。

### 升级既有项目

升级前先用当前旧版本完成一次 `recover`，不要带着未完成事务升级。0.3.0 不会用放宽规则
重放带非便携路径的旧事务，因为无法证明外部项目中的旧 manifest 可信；这类事务会持久标为
`NONPORTABLE_LEGACY_PATH` 并进入 `resolve_conflict`，需用旧版本恢复或人工迁移，不会反复崩溃。

0.3.0 新发布的路径不得包含控制字符、Windows 保留设备名、`< > : " | ? *`，分量也不得以
点或空格结尾，也不得与另一已跟踪路径只在大小写或 Unicode 规范化形式上不同。既有 POSIX
项目里同时存在 `Notes.md` / `notes.md` 或 NFC / NFD 等价文件时，先合并并重命名为唯一
拼写，再重新 publish / accept / review / package。

**已跟踪的旧拼写不阻断其他产物**：0.2.0 记进 `short-drama.json` 运行状态的 Win32 非法
拼写会在唯一性检查中跳过，项目其余部分照常发布——新路径本身必须通过同一道校验，不可能
与被跳过的拼写折叠成同一个文件。该拼写自身要重新发布时改名即可。RC 期间若由错误 owner
发布过 `_progress.md` 或 `chapters/*.md`，由 `$short-drama-novel-analyze` 重新发布并刷新
下游 hash。

旧产物里的跨产物引用若已经使用 `*_ref` / `*_refs`，重新发布前补齐 canonical
ArtifactRef 的 `owner`、`artifact` 与 `hash`。若它原本只是同文件内的记录 ID，改用
`*_id` / `*_ids`；若它只定位尚未发布的未来产物，改用不带 hash 的 `*_locator` /
`*_locators`。完成修订后重新 publish / accept，并刷新所有下游输入 hash。

若项目里已有 `episodes/ep1/` 这类旧目录，其中的产物仍可 `accept`，但不能再发布新版本，
也不能打包交付。迁移方式是把内容按 `episodes/EP001/` 重新发布一次并重新接受。交付枚举
按 `episodes/<EP>/` 前缀匹配，与负责人无关，所以旧路径本来就不在 `EP001` 的清点范围内，
不需要额外处理；磁盘上的旧文件不会被自动删除，确认新版本无误后自行清理。

`IMG-12` / `VID-17` 要求的 `slot_id` 与 `order` 是本次新增字段，0.2.0 及更早的项目一定没有。
只有**同一条记录里带两条及以上 `reference_bindings`** 的产物需要迁移，单条参考不受影响。
迁移只补字段、不改任何参考的含义：按现有数组顺序给每条参考写 `order`（`1`、`2`、`3`…），
再给每条写一个此后不再变动的 `slot_id`（如 `REF-IDENTITY`、`REF-COMPOSITION`），然后把该
产物重新发布并重新接受，让下游刷新 `hash`。补完之前，既有产物仍可读、可打包，只是再送
`$short-drama-review` 时会收到槽位未声明的 `reviewed_invariant` finding。

### 已知缺口

- 声明为 `structural_invariant` 的规则共 31 条，其中能在脚本里按规则编号追溯到自动校验器的是
  `SHT-16`、`SHT-17`、`VID-15` 与本次新增的 `VID-04`，共 4 条。其余规则是否已有部分机械
  覆盖、是否需要新增 validator 尚未逐条审计，不把“脚本未登记”误写成“只能语义判断”。
  新加的 `IMG-12` 与
  `VID-17` 暂列为 `reviewed_invariant`：本轮不为这一项新增匹配脚本，审查者按准确槽位与顺序
  给证据；将来只有真实项目校验器接管后才提升为结构阻断。
- `设定集/*.jsonl` 仍没有记录级 schema：发布检查只检查后缀、UTF-8 与可解析性，
  `{"a":1}` 与空文件都能发布进去。现有阶段校验脚本也都没有接进 `publish` 闸门，
  只在智能体按提示词执行时才运行。
- `verify` 的 `checksum_list_authentic` 用的是同一棵树内的第二个锚点
  （`.short-drama/state.json` 里记录的 hash）。把产物、校验和清单与状态文件一起改仍能
  得到 `intact`；要防这一类需要把 hash 留在项目之外，属于交付流程而非本工具的范围。

## [0.3.0-rc.1] - 2026-08-13

0.3.0 的首个公开发布候选。正式版在候选版之后补齐原著索引失败路径、分析产物所有权、
结构化引用完整性、VID-04 非法数值、跨平台路径唯一性、旧事务安全阻断及 Windows 交付
验证；没有新增创作功能或改变 Agent 主导的语义判断边界。预发布 GitHub Release 已由
正式版取代并删除，`v0.3.0-rc.1` 标签继续保留，供升级验证记录追溯。

## [0.2.0] - 2026-07-27

本次把 0.1.0 「已知缺口」中列出的全部条目处理完，给三条此前只靠人工核对的
`structural_invariant` 补上执行脚本，并修掉三处沿途发现的问题。

### 新增

- **可替换实现**（`SCR-10`）：平台审核导致的整集重做此前零覆盖。本条**不内置任何平台
  标准、不预测通过与否、不给题材禁令**，只处理一种可复现的失效——某个兑现只有一种实现
  方式，被下游一路消费之后必须更换，此时唯一可行的操作变成删掉这场，而它承担的功能
  无处兑现。做法是把功能、当前实现、备选实现分开写；备选须同一个人被改变、下游兑现与
  下一集进入状态仍成立、代价没被抹掉、不需要新铺垫。**规则在创作者标注前不生效**，
  且「提前把可能敏感处统一磨平」被明确列为更贵的错误。
- **角度、机位高度与焦段的词表**：景别原有受控词表，角度只有一句「有动机的角度」，
  三个彼此独立的选择被压进同一个词。三栏分开并写明归属：前两栏是镜头拥有的调度事实，
  焦段意图留在关键帧。
- **巧合与误会的可用条件**：`SCR-02` 说重大转向优先用选择，但从未说何时可用。补上
  方向性判据（制造问题成立、解决问题是欺骗）与误会的四条判据，其中最关键的是解除时
  必须有残余。
- **对白方法的反向配重**：十种方法各自的失效条件，用于修订时反查。
- **配音本模板**：补上录音顺序不是剧情顺序所丢失的上下文（对谁说、接谁的话、此刻知道
  什么、这一句要达成什么），并要求写策略而非情绪词。它是剧本的投影，不是第二份台词权威。
- **长篇改编的删线判据**，以及「先删线，再合并」的顺序理由。
- **亲子隐秘题材卡**（置信度中），与家庭关系卡按「关系是否已被承认」分界。

### 变更

- **共享输入的失效半径从整文件收窄到记录**。发布时可用
  `--input-record <path>=<selector>` 声明本候选实际读了哪几条记录（JSONL 用记录 ID，
  JSON 用 RFC 6901 指针）。`accepted_inputs` 中的整文件 hash 从此是绑定当时的快照，
  是否仍有效改由被绑定的记录判定，因此文件 hash 前进后产物依然可交付。未声明记录的
  输入行为不变；Markdown 不支持记录级绑定。收窄沿依赖链一路生效：直接下游存活时，
  它的目标根本不进受影响集合，间接下游不受牵连；而直接下游一旦要重做，它的字节即便
  没动也已不可信，下游照旧标 `stale`。
- **交付完整性由工具枚举**：本集 `episodes/<EP>/` 下每一个已接受路径必须出现在
  `--include` 或新增的 `--omit` 中，否则拒绝打包。`--omit` 会在清单的 `omitted` 段
  留痕，并区分「已就绪但未选」与「尚未就绪」。**既有的部分交付脚本需要补 `--omit`**。
- **单集时长两级算术兜底**：`SHT-16` 要求覆盖记录带 `episode_duration`，总和等于本集
  镜头 `duration_seconds` 的算术和，时长未定的镜头显式挂起而不是漏掉；
  `VID-15` 要求一集之内一个镜头最多属于一个容器，容器成员与散镜不重不漏覆盖本集镜头
  集合。目标时长的差值只报告不阻断。
- **尾帧契约**（`SHT-17`）：关键帧模板的 `start_boundary_ref` 改为 `boundary_role` +
  `boundary_ref`，使首尾帧接续有合规路径。尾帧是 `end_boundary` 的投影，不是第二个
  终点权威。`SHT-10` 相应改为按声明的边界判定。**既有关键帧记录需要补 `boundary_role`**。
- **`decided_by` 有了语义**：承担判断错误后果的一方——创作者本人，或有前置授权、不能
  自行扩权的受托人。助手、智能体、负责技能与审查者永远不是合法取值。
- **声明 Python 3.10 为版本下限**，创作者会调用的脚本在版本过低时直接说明原因。
- **套件版本升到 0.2.0**，七份 `suite-ref.json` 随之更新；混装旧版子技能会被安装验证
  拒绝。

### 修复

- `project_tool.py` 依赖 `datetime.UTC`（3.11+），而仓库从未声明版本下限，macOS 自带的
  3.9 直接导入失败。已改用 `timezone.utc` 并把下限降到 3.10。
- `accept` 的命令示例与所有权表仍在教单文件 `creator-decisions.jsonl`，正是 0.1.0
  判定为静默失效的布局；照它做会重新引入该缺陷。
- `quick_validate` 测试把「校验器自己起不来」当成「技能不合规」。校验器在仓库外并带
  自己的第三方依赖，缺依赖时 9 个测试变红而技能其实合规。
- 清单工具只把 `core_manifest_sha256` 同步给七份 `suite-ref.json`，版本号不同步。
  它是文档里保持一致的唯一命令，于是升版本会留下半套旧引用——安装验证虽能拒绝，
  但已经是发布之后的事了。现在版本相关字段一并下发。

- **三条 `structural_invariant` 有了脚本执行者**。`SHT-16` 与 `SHT-17` 由分镜技能的
  `storyboard_check.py` 执行，`VID-15` 由视频提示词技能的 `container_check.py` 执行，
  各自的诊断码登记在所属技能的诊断目录里。它们只做算术与集合比对，不评价镜头好坏。
  两处刻意不判错：目标时长的差值大小、以及每镜关键帧的数量。
- **配音本有了结构校验**：`voice_sheet_check.py` 按块 ID 定位、切出剧本原字节、核对
  内容 hash，再逐字比对台词与说话人。它区分「表里改了词」与「剧本改了而索引没重建」，
  因为这两种情况下的配音本看起来都和正常的一模一样。未覆盖的对白块只报告不判错。

### 已知缺口

- **发布门禁三项仍未完成**：fresh-agent 双臂盲测、独立 reviewer verdict 与
  protected-release gate。本轮新增规则由套件自身契约与工艺推导而来，不涉及非公开来源，
  因此不适用 promotion ledger 的去标识流程；但独立审查这一项对任何来源都成立，
  且候选作者不能自审——这一项无法由本轮工作自行满足。
- 三个新校验器只覆盖**可机器判定**的那一部分。镜头拆得对不对、容器分得合不合理、
  台词读起来顺不顺，仍然是审查者的工作，脚本通过不等于产物成立。

## [0.1.0] - 2026-07-26

首个公开版本。八个同级技能：路由 `short-drama` 与 develop / write / assets /
image-prompts / storyboard / video-prompts / review。

### 新增

- **完整生产链路**：从点子或长材料到分集剧本、资产设定、图片提示词、分镜与关键帧、
  视频提示词、独立审查记录，各环节用同一套创作者决策、来源引用与连续性契约衔接。
- **规则四分级**：`structural_invariant`（可机器校验、可阻断）、`reviewed_invariant`
  （需证据判断）、`craft_default`（可覆盖的常用做法）、`taste_option`（创作者选择，
  不得单独阻断）。字数、镜头数、情绪曲线、动作密度**永远不是**结构不变式。
- **87 条规则**，稳定 ID 前缀 `STY`/`SCR`/`AST`/`IMG`/`SHT`/`VID`/`CON`/`REV`，
  由拥有该前缀的技能在自己的 `stage-contract.md` 中维护。
- **10 张题材卡**（复仇打脸、古装权谋、悬疑规则、家庭关系、职场喜剧、身份错位、
  生活流、仙侠修真、豪门婚恋、动作任务）与 **6 张制作形态卡**（实拍、二维动态漫、
  风格化三维、水墨笔触、Q 版表达、国漫二次元）。
- **前提装置层**（`STY-17`）：把「主角带着已知结果开局」与「获得外部授权能力」处理成
  叠加在题材之上的一层，并把**创作者契约**（可完整而暂不披露）与**剧中披露**
  （可残缺、可被误述）分开——观众此刻不知道全部边界从不构成缺陷。
- **交付容器分层**（`VID-13`）：容器可承载一个或多个已接受镜头，时长两级精确加总；
  成员须来源顺序连续、共享同一绑定链、不跨场次。
- **独立审查机制**：负责人不得自审；取不到独立上下文时降级为 `PROVISIONAL` 而不是
  假装通过。
- **确定性生命周期工具**：`init` / `status` / `recover` / `publish` / `accept` /
  `review` / `package`，只做 hash、索引、事务与清单，不做创作判断。

### 变更

- **每个技能自包含**：子技能不再读取彼此或核心的文件；共享内容按阶段切片内联到各自的
  `references/stage-contract.md`，核心索引改为按技能名路由。代价是 `CON-*` 在三处各存
  一份，由测试强制正文一致。
- **创作者决定与审查证据按产物分文件**（`creator-decisions/<artifact-id>.json`）。
  此前默认的单一追加式 `creator-decisions.jsonl` 与整文件 hash 绑定不兼容：接受第二个
  产物会使第一个产物的证据引用永久指向不再存在的字节状态。该失效是静默的——命令不报错，
  链条看起来完好，但除最近一次外的所有接受证据都无法再复核。

### 修复

- `STY-13` 此前写「目标、阻碍**或**选择产生局部结果」，使主角能动性成为可选项；
  主角全程被动、由外力送上翻盘的一集可以合法通过全部检查。现要求局部结果可追溯到焦点
  人物的一次选择，外力送达时须写清促成它的既有动作与主角的处置。
- 全套件此前未约定裸写的「左/右」指画面还是人物，两份资料各按一种读法书写。现定为
  画面左右，人物自身的左右必须带主体。这是正反打方向翻转最常见的成因，且单看每一句
  都成立，文字审查抓不到。
- `VID-04` 与 `VID-13` 的措辞未区分「镜头时长」与「容器时长」，两级算术存在歧义。
- 交付容器与运动规格此前互相携带对方的文件 hash，构成永远无法收敛的循环；依赖方向
  已改为单向 `container → motion → shot`。

### 已知缺口

以下问题已定位但未在本版本处理，按影响排序：

- **共享文件的失效半径按路径而非记录**：`bible/*.jsonl` 与 `short-drama.json` 是全项目
  共享输入，任何增补会把引用它们的全部下游产物标记 `stale`。文档承诺的「只标记依赖该
  ID/variant 的产物」目前实现不到。规避做法是把 bible 按弧线分片并规划资产增补窗口。
- **内容合规与风险标注零覆盖**：平台审核导致的整集重做是真实短剧最贵的返工来源之一，
  套件目前没有对应位置。
- **单集总时长没有算术兜底**：`target_seconds_per_episode` 字段存在但无消费方；
  `STY-16` 的容量预算在第一集因无基准可采样而跳过。
- **默认每镜一张关键帧，且尾帧契约未定**，AI 视频常用的首尾帧接续工作流没有合规路径。
- **交付包完整性靠人工枚举**：`package` 唯一的检查是选择不为空。
- 角度、机位高度与焦段没有词表（景别有）；配音本、`decided_by` 的角色语义、
  对白方法的反向配重、误会与巧合的可用条件、长篇改编的删线判据、亲子题材卡均待补。

### 边界

本版本只交付文本与结构化记录。不生成图片、视频或音频，不调用生成服务或供应商接口，
不在产物中出现供应商与模型产品名，不从提示词或任务状态声称成片质量，也不做分发与
效果度量。审查环节只读文本，不查看渲染媒体。

### 发布门禁说明

`skills/` 下的知识来自受控的定性研究流程，示例一律合成改写。本版本中，
fresh-agent 双臂盲测、独立 reviewer verdict 与 protected-release gate 三项未完成，
由维护者知情后放行；相应记录以 `hold` 而非 `promotion` 留在仓库外的受控工作区。

[Unreleased]: https://github.com/zenstory-ai/drama-skills/compare/v0.6.6...HEAD
[0.6.6]: https://github.com/zenstory-ai/drama-skills/compare/v0.6.5...v0.6.6
[0.6.5]: https://github.com/zenstory-ai/drama-skills/compare/v0.6.4...v0.6.5
[0.6.4]: https://github.com/zenstory-ai/drama-skills/compare/v0.6.3...v0.6.4
[0.6.3]: https://github.com/zenstory-ai/drama-skills/compare/v0.6.2...v0.6.3
[0.6.2]: https://github.com/zenstory-ai/drama-skills/compare/v0.6.1...v0.6.2
[0.6.1]: https://github.com/zenstory-ai/drama-skills/compare/v0.6.0...v0.6.1
[0.6.0]: https://github.com/zenstory-ai/drama-skills/compare/v0.5.0...v0.6.0
[0.5.0]: https://github.com/zenstory-ai/drama-skills/compare/v0.4.2...v0.5.0
[0.4.2]: https://github.com/zenstory-ai/drama-skills/compare/v0.4.1...v0.4.2
[0.4.1]: https://github.com/zenstory-ai/drama-skills/compare/v0.4.0...v0.4.1
[0.4.0]: https://github.com/zenstory-ai/drama-skills/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/zenstory-ai/drama-skills/compare/v0.2.0...v0.3.0
[0.3.0-rc.1]: https://github.com/zenstory-ai/drama-skills/compare/v0.2.0...v0.3.0-rc.1
[0.2.0]: https://github.com/zenstory-ai/drama-skills/releases/tag/v0.2.0
[0.1.0]: https://github.com/zenstory-ai/drama-skills/releases/tag/v0.1.0
